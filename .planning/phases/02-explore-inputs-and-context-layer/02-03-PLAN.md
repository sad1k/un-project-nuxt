---
phase: 02-explore-inputs-and-context-layer
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01-PLAN.md
  - 02-02-PLAN.md
files_modified:
  - server/api/explore/context.get.ts
  - server/api/explore/candidate-places.get.ts
  - lib/db/queries/explore-context.ts
  - components/explore/context-selector.vue
  - components/explore/candidate-places.vue
  - components/explore/route-panel.vue
  - composables/use-current-location.ts
  - composables/use-explore-context.ts
  - tests/server/explore-context.test.mjs
  - .planning/phases/02-explore-inputs-and-context-layer/02-HANDOFF.md
autonomous: true
requirements:
  - EXPIN-04
  - EXPIN-05
user_setup:
  - "Browser geolocation must be tested manually because permission prompts cannot be fully validated by server tests."
must_haves:
  truths:
    - "Saved places, diary logs, and current location are explicit context choices, not silently included personal history."
    - "Candidate/popular places appear as first-visit or selected-city context without fabricating popularity or community signals."
    - "Persistent generation history remains deferred to Phase 3; Phase 2 only prepares hooks/state for it."
    - "Covers CONTEXT decisions D-10, D-11, D-12, D-13, D-14, and D-15."
  artifacts:
    - path: "server/api/explore/context.get.ts"
      provides: "Bounded user-owned saved place and diary-log context"
      contains: "defineAuthenticatedHandler"
    - path: "server/api/explore/candidate-places.get.ts"
      provides: "Candidate/popular place suggestions for selected city"
      contains: "ExploreCandidatePlace"
    - path: "components/explore/context-selector.vue"
      provides: "Explicit current-location/saved-place/diary-log selection UI"
      contains: "currentLocation"
    - path: "components/explore/candidate-places.vue"
      provides: "First-visit/selected-city candidate place UI"
      contains: "candidate"
    - path: "tests/server/explore-context.test.mjs"
      provides: "Focused tests for bounded personal-context shaping"
      min_lines: 20
  key_links:
    - from: "server/api/explore/context.get.ts"
      to: "lib/db/queries/explore-context.ts"
      via: "endpoint reads only authenticated user's saved locations/log summaries"
      pattern: "userId"
    - from: "components/explore/context-selector.vue"
      to: "composables/use-explore-context.ts"
      via: "selected personal context ids flow into ExploreRequestContext"
      pattern: "selectedSavedPlaceIds"
---

<objective>
Add explicit personal context selection and candidate/popular places to complete the Phase 2 input layer.

Purpose: The AI route phase needs bounded user context and candidate place inputs, but Phase 2 must not silently send all diary history or implement AI conversation persistence.
Output: Authenticated context endpoint, current-location opt-in, saved-place/diary-log selectors, candidate/popular-place UI, and final request-context verification.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-explore-inputs-and-context-layer/02-CONTEXT.md
@.planning/phases/02-explore-inputs-and-context-layer/02-RESEARCH.md
@.planning/phases/02-explore-inputs-and-context-layer/02-01-PLAN.md
@.planning/phases/02-explore-inputs-and-context-layer/02-02-PLAN.md
@stores/location.ts
@lib/db/queries/location.ts
@lib/db/schema/location.ts
@lib/db/schema/location-log.ts
@composables/use-explore-context.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bounded Explore personal-context endpoint</name>
  <files>server/api/explore/context.get.ts, lib/db/queries/explore-context.ts</files>
  <read_first>lib/db/queries/location.ts, lib/db/schema/location.ts, lib/db/schema/location-log.ts, utils/define-authenticated-handler.ts, .planning/phases/02-explore-inputs-and-context-layer/02-CONTEXT.md</read_first>
  <action>Create query helpers and an authenticated endpoint that return only bounded saved-place and diary-log summaries for the current user. Include ids, names, coordinates, and short descriptions/dates needed for user selection. Do not return images, full diary text, provider secrets, or another user's records. Use explicit `userId` filters in the query layer. Keep response shape aligned with `ExploreRequestContext` and Phase 3 context needs.</action>
  <verify>
    <automated>rg --fixed-strings "defineAuthenticatedHandler" server/api/explore/context.get.ts</automated>
    <automated>rg --fixed-strings "userId" lib/db/queries/explore-context.ts server/api/explore/context.get.ts</automated>
  </verify>
  <acceptance_criteria>
    - Endpoint returns saved places and diary logs scoped to authenticated user.
    - Response is bounded and excludes full private diary payloads/images.
    - Query helper takes `userId` explicitly.
  </acceptance_criteria>
  <done>Explore can fetch personal context safely and explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Add current-location opt-in helper</name>
  <files>composables/use-current-location.ts, composables/use-explore-context.ts</files>
  <read_first>composables/use-explore-context.ts, .planning/phases/02-explore-inputs-and-context-layer/02-CONTEXT.md</read_first>
  <action>Create `use-current-location.ts` for browser geolocation opt-in. It should expose permission/request state, coordinates when available, denied/unavailable/error states, and a clear reset/disable path. Integrate it with `use-explore-context.ts` so current location is included in `ExploreRequestContext` only after explicit opt-in and successful coordinates. Guard browser-only APIs for Nuxt client execution.</action>
  <verify>
    <automated>rg --fixed-strings "navigator.geolocation" composables/use-current-location.ts</automated>
    <automated>rg --fixed-strings "currentLocation" composables/use-explore-context.ts composables/use-current-location.ts</automated>
  </verify>
  <acceptance_criteria>
    - Current location is opt-in, never automatic on page load.
    - Denied/unavailable/error states can be shown by UI.
    - Request context omits current coordinates unless opt-in succeeds.
  </acceptance_criteria>
  <done>Current location is available as safe optional route context.</done>
</task>

<task type="auto">
  <name>Task 3: Add explicit context selector UI</name>
  <files>components/explore/context-selector.vue, components/explore/route-panel.vue, composables/use-explore-context.ts</files>
  <read_first>server/api/explore/context.get.ts, composables/use-current-location.ts, composables/use-explore-context.ts, components/explore/route-panel.vue</read_first>
  <action>Create `context-selector.vue` and compose it into `route-panel.vue`. The selector should let the user enable current location, choose saved places, and choose prior diary logs from the bounded context endpoint. It should make selection explicit and understandable, with loading/empty/error states. It should update `useExploreContext()` selected ids and current-location state. Do not auto-select all saved places/logs.</action>
  <verify>
    <automated>rg --fixed-strings "/api/explore/context" components/explore/context-selector.vue</automated>
    <automated>rg --fixed-strings "selectedSavedPlaceIds" components/explore/context-selector.vue composables/use-explore-context.ts</automated>
    <automated>rg --fixed-strings "selectedDiaryLogIds" components/explore/context-selector.vue composables/use-explore-context.ts</automated>
  </verify>
  <acceptance_criteria>
    - User can explicitly include/exclude current location.
    - User can explicitly choose saved places and diary logs.
    - Empty state tells the user when no saved context is available.
    - Context selector does not implement AI generation or history persistence.
  </acceptance_criteria>
  <done>Personal context selection is user-facing and connected to typed request context.</done>
</task>

<task type="auto">
  <name>Task 4: Add candidate/popular places for first visit and selected city</name>
  <files>server/api/explore/candidate-places.get.ts, components/explore/candidate-places.vue, components/explore/route-panel.vue, composables/use-explore-context.ts</files>
  <read_first>lib/explore/search.ts, lib/explore/context.ts, server/api/explore/city-retrieve.get.ts, .planning/phases/02-explore-inputs-and-context-layer/02-RESEARCH.md</read_first>
  <action>Add candidate/popular-place support without fabricating popularity. The endpoint should accept a selected city/provider context and return normalized candidate places from Mapbox/provider data when available, or an empty/fallback state when unavailable. If app-owned saved/community data is used, label it as app-derived. The UI should show useful first-visit or after-city-selection candidates and allow existing filters to refine them. Selected candidate ids/items should flow into `ExploreRequestContext` for Phase 3.</action>
  <verify>
    <automated>rg --fixed-strings "candidate" server/api/explore/candidate-places.get.ts components/explore/candidate-places.vue composables/use-explore-context.ts</automated>
    <automated>rg --fixed-strings "ExploreRequestContext" composables/use-explore-context.ts</automated>
  </verify>
  <acceptance_criteria>
    - Candidate/popular places appear when provider/app data supports them.
    - UI avoids unsupported claims like live popularity or current crowd presence.
    - Candidate places are filterable through Phase 2 filter state.
    - Candidate context is included in the typed request context.
  </acceptance_criteria>
  <done>Explore has first-visit candidate content and route-context candidates without overclaiming provider/app data.</done>
</task>

<task type="auto">
  <name>Task 5: Add focused personal-context tests and final handoff check</name>
  <files>tests/server/explore-context.test.mjs, .planning/phases/02-explore-inputs-and-context-layer/02-HANDOFF.md</files>
  <read_first>tests/server/README.md, lib/db/queries/explore-context.ts, lib/explore/context.ts, .planning/phases/02-explore-inputs-and-context-layer/02-CONTEXT.md</read_first>
  <action>Add focused server tests for pure context shaping and ownership-safety helper behavior. Use local fixtures and no real database/network calls unless the query helper is structured for isolated testing. Also create `02-HANDOFF.md` summarizing the final `ExploreRequestContext` fields that Phase 3 must consume, what remains deliberately deferred, and manual browser checks needed for city typeahead/current-location UX.</action>
  <verify>
    <automated>node scripts/run-node-tests.mjs tests/server</automated>
    <automated>rg --fixed-strings "ExploreRequestContext" .planning/phases/02-explore-inputs-and-context-layer/02-HANDOFF.md</automated>
    <automated>rg --fixed-strings "Phase 3" .planning/phases/02-explore-inputs-and-context-layer/02-HANDOFF.md</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover bounded context shaping and selected ids.
    - Handoff document names every request-context field expected by Phase 3.
    - Handoff explicitly states generation history persistence remains Phase 3.
  </acceptance_criteria>
  <done>Phase 2 ends with tested context shaping and a clear AI-route handoff.</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/run-node-tests.mjs tests/server` passes.
- [ ] `rg --fixed-strings "defineAuthenticatedHandler" server/api/explore/context.get.ts` passes.
- [ ] `rg --fixed-strings "navigator.geolocation" composables/use-current-location.ts` passes.
- [ ] `rg --fixed-strings "ExploreRequestContext" .planning/phases/02-explore-inputs-and-context-layer/02-HANDOFF.md` passes.
- [ ] Manual browser check is recorded for city typeahead, current-location denied state, saved context selection, and candidate places.
</verification>

<success_criteria>

- EXPIN-04 is covered by candidate/filter integration.
- EXPIN-05 is covered by current-location, saved-place, and diary-log selection.
- D-10 through D-15 are represented in executable tasks.
- Persistent generation history remains explicitly deferred to Phase 3.

</success_criteria>

<output>
After completion, create `.planning/phases/02-explore-inputs-and-context-layer/02-03-SUMMARY.md`
</output>
