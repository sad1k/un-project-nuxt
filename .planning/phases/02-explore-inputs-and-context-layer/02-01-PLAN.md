---
phase: 02-explore-inputs-and-context-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/explore/search.ts
  - lib/explore/context.ts
  - server/api/explore/city-suggest.get.ts
  - server/api/explore/city-retrieve.get.ts
  - server/api/search-locations.get.ts
  - lib/db/schema/search-location.ts
  - tests/server/explore-search.test.mjs
autonomous: true
requirements:
  - EXPIN-01
  - EXPIN-04
user_setup:
  - "Mapbox Search Box behavior requires a configured MAPBOX_TOKEN. If the token is missing or unsuitable for search, the Nominatim fallback must still work."
must_haves:
  truths:
    - "City search is live-typeahead-ready and Mapbox-first when provider configuration is available."
    - "Nominatim remains a hardened fallback, not the preferred high-frequency autocomplete provider."
    - "Search and selected-city data use a typed provider-neutral shape for later Explore request context."
    - "Covers CONTEXT decisions D-01, D-02, D-03, D-04, D-05, and D-07."
  artifacts:
    - path: "lib/explore/search.ts"
      provides: "Provider-neutral Explore search types and normalization helpers"
      contains: "ExploreCitySuggestion"
    - path: "lib/explore/context.ts"
      provides: "Typed Explore request context seed used by later plans"
      contains: "ExploreRequestContext"
    - path: "server/api/explore/city-suggest.get.ts"
      provides: "Mapbox-first city suggestion endpoint with fallback behavior"
      contains: "URLSearchParams"
    - path: "server/api/explore/city-retrieve.get.ts"
      provides: "Selected-city retrieve/normalization endpoint"
      contains: "mapbox"
    - path: "server/api/search-locations.get.ts"
      provides: "Hardened Nominatim fallback"
      contains: "URLSearchParams"
    - path: "tests/server/explore-search.test.mjs"
      provides: "Focused tests for query normalization and provider result shaping"
      min_lines: 20
  key_links:
    - from: "server/api/explore/city-suggest.get.ts"
      to: "lib/explore/search.ts"
      via: "endpoint returns normalized ExploreCitySuggestion objects"
      pattern: "ExploreCitySuggestion"
    - from: "server/api/explore/city-retrieve.get.ts"
      to: "lib/explore/search.ts"
      via: "selected city is normalized before UI state uses it"
      pattern: "SelectedExploreCity"
---

<objective>
Build the provider and typing foundation for Explore city autocomplete and candidate filtering.

Purpose: Phase 2 needs live city typeahead and a clean request-context boundary before UI and AI work build on it.
Output: Mapbox-first search endpoints/utilities, hardened Nominatim fallback, provider-neutral types, and focused server tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-explore-inputs-and-context-layer/02-CONTEXT.md
@.planning/phases/02-explore-inputs-and-context-layer/02-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@server/api/search-locations.get.ts
@lib/db/schema/search-location.ts
@lib/env.ts
@lib/types.ts
@tests/server/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Explore search and request-context types</name>
  <files>lib/explore/search.ts, lib/explore/context.ts</files>
  <read_first>.planning/phases/02-explore-inputs-and-context-layer/02-CONTEXT.md, .planning/phases/02-explore-inputs-and-context-layer/02-RESEARCH.md, lib/types.ts</read_first>
  <action>Create `lib/explore/search.ts` with provider-neutral types and pure helpers for Explore search: `ExploreSearchProvider`, `ExploreCitySuggestion`, `SelectedExploreCity`, normalized query helpers, and normalization helpers for Mapbox and Nominatim-like result shapes. Create `lib/explore/context.ts` with an `ExploreRequestContext` type that includes selected city, days, interests, filters, current-location opt-in/current coordinates, selected saved place ids, selected diary log ids, and candidate place ids/items. Keep this file free of Vue refs and browser APIs. Do not read `.env`, do not add dependencies, and do not expose provider secrets.</action>
  <verify>
    <automated>Test-Path lib/explore/search.ts</automated>
    <automated>Test-Path lib/explore/context.ts</automated>
    <automated>rg --fixed-strings "ExploreCitySuggestion" lib/explore/search.ts</automated>
    <automated>rg --fixed-strings "ExploreRequestContext" lib/explore/context.ts</automated>
  </verify>
  <acceptance_criteria>
    - Provider-neutral types support both Mapbox and Nominatim.
    - Selected city stores display name, coordinates, provider, and provider id when available.
    - Request context type is serializable and independent of UI component internals.
  </acceptance_criteria>
  <done>Explore search and request-context data shapes exist before endpoints or UI consume them.</done>
</task>

<task type="auto">
  <name>Task 2: Add Mapbox-first city suggest and retrieve endpoints</name>
  <files>server/api/explore/city-suggest.get.ts, server/api/explore/city-retrieve.get.ts</files>
  <read_first>lib/explore/search.ts, lib/env.ts, server/api/search-locations.get.ts, .planning/phases/02-explore-inputs-and-context-layer/02-RESEARCH.md</read_first>
  <action>Add authenticated Nitro endpoints for Explore city search. `city-suggest.get.ts` should validate query text with Zod, enforce a minimum useful query length, normalize whitespace, and use Mapbox Search Box suggest when `MAPBOX_TOKEN` is configured. It should return normalized suggestions and fall back to Nominatim normalization when Mapbox is unavailable or returns a recoverable provider failure. `city-retrieve.get.ts` should retrieve a selected Mapbox suggestion by id/session token and return `SelectedExploreCity`; for fallback providers, it should accept enough suggestion data to normalize the already-selected city without a second provider call. Use `URL`/`URLSearchParams`, avoid `console.log`, return sanitized errors, and keep implementation compatible with existing auth handler patterns.</action>
  <verify>
    <automated>rg --fixed-strings "URLSearchParams" server/api/explore/city-suggest.get.ts server/api/explore/city-retrieve.get.ts</automated>
    <automated>rg --fixed-strings "defineAuthenticatedHandler" server/api/explore/city-suggest.get.ts server/api/explore/city-retrieve.get.ts</automated>
    <automated>rg --fixed-strings "ExploreCitySuggestion" server/api/explore/city-suggest.get.ts</automated>
    <automated>rg --fixed-strings "SelectedExploreCity" server/api/explore/city-retrieve.get.ts</automated>
  </verify>
  <acceptance_criteria>
    - Suggest endpoint returns normalized city suggestions for Mapbox and fallback paths.
    - Retrieve endpoint returns one selected city with coordinates and provider identity.
    - Provider failures are sanitized and do not leak tokens, URLs with credentials, prompts, or raw headers.
    - No new package dependency is introduced.
  </acceptance_criteria>
  <done>Explore has a Mapbox-first city autocomplete API boundary ready for UI integration.</done>
</task>

<task type="auto">
  <name>Task 3: Harden existing Nominatim fallback</name>
  <files>server/api/search-locations.get.ts, lib/db/schema/search-location.ts</files>
  <read_first>server/api/search-locations.get.ts, lib/db/schema/search-location.ts, .planning/phases/02-explore-inputs-and-context-layer/02-RESEARCH.md</read_first>
  <action>Update the existing Nominatim search route so it is safe as a fallback: validate trimmed query length, build the upstream URL with `URL` and `URLSearchParams`, normalize the Nitro cache key, remove direct debug logging, and return sanitized provider errors. Keep the existing route for compatibility with `components/app/search-locations.vue`, but make its behavior align with the new fallback constraints. Tighten `SearchLocationQuery` if needed, without breaking existing dashboard/location search behavior.</action>
  <verify>
    <automated>rg --fixed-strings "URLSearchParams" server/api/search-locations.get.ts</automated>
    <automated>if (Select-String -Path server/api/search-locations.get.ts -Pattern "console\\.log" -Quiet) { throw "console.log still present" }</automated>
    <automated>rg --fixed-strings "getKey" server/api/search-locations.get.ts</automated>
  </verify>
  <acceptance_criteria>
    - Raw query interpolation is removed.
    - Cache keys normalize whitespace/case enough to avoid obvious duplicate cache entries.
    - Route no longer emits debug logs.
    - Existing search UI can still call `/api/search-locations`.
  </acceptance_criteria>
  <done>Nominatim fallback is safe enough for limited fallback use.</done>
</task>

<task type="auto">
  <name>Task 4: Add focused provider-shaping tests</name>
  <files>tests/server/explore-search.test.mjs</files>
  <read_first>tests/server/README.md, scripts/run-node-tests.mjs, lib/explore/search.ts, lib/explore/context.ts</read_first>
  <action>Add `tests/server/explore-search.test.mjs` using Node's built-in `node:test` and `node:assert/strict`. Test pure helper behavior only: query normalization, cache-key normalization if exported, Mapbox suggestion normalization, Nominatim suggestion normalization, selected-city shaping, and `ExploreRequestContext` serializability expectations. Do not call real Mapbox, Nominatim, Turso, or `.env`.</action>
  <verify>
    <automated>node scripts/run-node-tests.mjs tests/server</automated>
  </verify>
  <acceptance_criteria>
    - Server tests run without network calls or provider credentials.
    - Tests cover both Mapbox and Nominatim normalization.
    - Tests fail if normalized city suggestions omit coordinates or provider identity.
  </acceptance_criteria>
  <done>Provider and context normalization has a focused regression test lane.</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/run-node-tests.mjs tests/server` passes.
- [ ] `rg --fixed-strings "URLSearchParams" server/api/explore/city-suggest.get.ts server/api/explore/city-retrieve.get.ts server/api/search-locations.get.ts` passes.
- [ ] `rg --fixed-strings "ExploreRequestContext" lib/explore/context.ts` passes.
- [ ] No new dependency appears in `package.json`.
</verification>

<success_criteria>

- EXPIN-01 is covered by Mapbox-first city suggestion/retrieve foundation.
- EXPIN-04 is partially covered by provider-neutral candidate/filter-ready data shapes.
- D-01 through D-05 and D-07 are represented in executable tasks.
- Provider failures are safe, typed, and testable.

</success_criteria>

<output>
After completion, create `.planning/phases/02-explore-inputs-and-context-layer/02-01-SUMMARY.md`
</output>
