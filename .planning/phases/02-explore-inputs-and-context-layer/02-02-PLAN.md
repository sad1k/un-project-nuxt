---
phase: 02-explore-inputs-and-context-layer
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01-PLAN.md
files_modified:
  - pages/explore.vue
  - components/explore/route-panel.vue
  - components/explore/city-typeahead.vue
  - components/explore/trip-preferences.vue
  - components/explore/place-filters.vue
  - components/explore/header-overlay.vue
  - components/explore/quick-actions.vue
  - components/explore/map-view.client.vue
  - components/explore/route-marker.ts
  - composables/use-explore-context.ts
  - composables/use-route-generator.ts
autonomous: true
requirements:
  - EXPIN-01
  - EXPIN-02
  - EXPIN-03
  - EXPIN-04
user_setup: []
must_haves:
  truths:
    - "RoutePanel and related Explore UI are refactored toward the design system rather than preserving the prototype as final UI."
    - "City, days, interests, and filters update a typed Explore context boundary instead of raw AI request state."
    - "Explore prototype filename blockers are removed or replaced for files touched by this plan."
    - "Covers CONTEXT decisions D-06, D-07, D-08, and D-09."
  artifacts:
    - path: "components/explore/city-typeahead.vue"
      provides: "Live city autocomplete UI"
      contains: "/api/explore/city-suggest"
    - path: "components/explore/trip-preferences.vue"
      provides: "Days and interests controls"
      contains: "selectedDays"
    - path: "components/explore/place-filters.vue"
      provides: "Reusable filter controls for candidate and generated places"
      contains: "filters"
    - path: "components/explore/route-panel.vue"
      provides: "Composed Explore input panel"
      contains: "ExploreCityTypeahead"
    - path: "composables/use-explore-context.ts"
      provides: "Vue-facing context state boundary"
      contains: "ExploreRequestContext"
  key_links:
    - from: "components/explore/city-typeahead.vue"
      to: "server/api/explore/city-suggest.get.ts"
      via: "typeahead fetches normalized suggestions"
      pattern: "city-suggest"
    - from: "composables/use-explore-context.ts"
      to: "lib/explore/context.ts"
      via: "UI context state uses serializable request type"
      pattern: "ExploreRequestContext"
---

<objective>
Refactor the Explore input UI around live autocomplete, trip preferences, filters, and typed request context.

Purpose: The current Explore panel is a visual prototype. Phase 2 needs production-shaped controls that gather route inputs without binding future AI generation to component internals.
Output: Design-system-aligned Explore input components, live city typeahead integration, day/interest/filter controls, and a Vue composable/store for typed request context.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-explore-inputs-and-context-layer/02-CONTEXT.md
@.planning/phases/02-explore-inputs-and-context-layer/02-RESEARCH.md
@.planning/phases/02-explore-inputs-and-context-layer/02-01-PLAN.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@pages/explore.vue
@components/explore/
@composables/useRouteGenerator.ts
@lib/explore/context.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize Explore prototype filenames and imports</name>
  <files>components/explore/header-overlay.vue, components/explore/quick-actions.vue, components/explore/map-view.client.vue, components/explore/route-marker.ts, pages/explore.vue, composables/use-route-generator.ts</files>
  <read_first>.planning/phases/01-explore-scope-and-verification-foundation/01-VERIFICATION-BASELINE.md, .planning/codebase/CONVENTIONS.md, pages/explore.vue, components/explore/</read_first>
  <action>Rename or replace the touched Explore prototype files so they use kebab-case filenames: `HeaderOverlay.vue` -> `header-overlay.vue`, `QuickActions.vue` -> `quick-actions.vue`, `MapView.client.vue` -> `map-view.client.vue`, `RouteMarker.ts` -> `route-marker.ts`, and `useRouteGenerator.ts` -> `use-route-generator.ts` if the mock is still retained. Update imports and Nuxt component usages accordingly. Preserve product intent but fix obvious mojibake/user-facing prototype strings when touching the files. Do not start Phase 4 route animation work beyond preserving existing map display compatibility.</action>
  <verify>
    <automated>Get-ChildItem components/explore | Select-Object -ExpandProperty Name | Select-String -Pattern "^[A-Z]" -Quiet | ForEach-Object { if ($_){ throw "PascalCase Explore filename remains" } }</automated>
    <automated>Select-String -Path pages/explore.vue,components/explore/* -Pattern "ExploreMapView" -SimpleMatch -ErrorAction SilentlyContinue</automated>
  </verify>
  <acceptance_criteria>
    - Touched Explore prototype files use kebab-case filenames.
    - `pages/explore.vue` still renders the Explore page shell.
    - Phase 4 map route behavior is not expanded in this task.
  </acceptance_criteria>
  <done>Explore files touched by Phase 2 no longer preserve known filename blockers.</done>
</task>

<task type="auto">
  <name>Task 2: Create Vue-facing Explore context state</name>
  <files>composables/use-explore-context.ts</files>
  <read_first>lib/explore/context.ts, lib/explore/search.ts, .planning/phases/02-explore-inputs-and-context-layer/02-CONTEXT.md</read_first>
  <action>Create `composables/use-explore-context.ts` as the UI state boundary for Phase 2. It should expose selected city, selected days, interests, filters, current-location opt-in state, selected saved place ids, selected diary log ids, candidate places, and a computed serializable `ExploreRequestContext`. Use Vue refs/computed internally but keep the exported request context compatible with `lib/explore/context.ts`. Provide reset/update helpers that UI components can call. Do not include AI provider calls, streamed responses, or persisted generation history.</action>
  <verify>
    <automated>rg --fixed-strings "ExploreRequestContext" composables/use-explore-context.ts</automated>
    <automated>rg --fixed-strings "computed" composables/use-explore-context.ts</automated>
  </verify>
  <acceptance_criteria>
    - Context composable exposes a serializable request-context computed value.
    - Days, interests, and filters are first-class state, not local-only route panel variables.
    - No AI endpoint or generation history persistence is implemented.
  </acceptance_criteria>
  <done>Explore UI has a typed state boundary ready for Phase 3.</done>
</task>

<task type="auto">
  <name>Task 3: Implement live city typeahead component</name>
  <files>components/explore/city-typeahead.vue</files>
  <read_first>server/api/explore/city-suggest.get.ts, server/api/explore/city-retrieve.get.ts, lib/explore/search.ts, composables/use-explore-context.ts</read_first>
  <action>Create a design-system-aligned `city-typeahead.vue` component. It should debounce user input, avoid provider calls below the minimum query length, call `/api/explore/city-suggest`, display loading/empty/error states, and call retrieve/selection logic when the user selects a city. It should update `useExploreContext()` with a normalized selected city. Keep keyboard and pointer selection usable. Do not expose raw provider tokens in client code.</action>
  <verify>
    <automated>rg --fixed-strings "/api/explore/city-suggest" components/explore/city-typeahead.vue</automated>
    <automated>rg --fixed-strings "selectedCity" components/explore/city-typeahead.vue composables/use-explore-context.ts</automated>
  </verify>
  <acceptance_criteria>
    - User can type and see city suggestions when enough characters are entered.
    - Selecting a suggestion stores normalized city data in Explore context.
    - Loading, empty, and provider-error states are visible without breaking the panel.
  </acceptance_criteria>
  <done>City autocomplete is user-facing and connected to the provider foundation.</done>
</task>

<task type="auto">
  <name>Task 4: Implement trip preferences and filters</name>
  <files>components/explore/trip-preferences.vue, components/explore/place-filters.vue, components/explore/route-panel.vue, pages/explore.vue</files>
  <read_first>components/explore/RoutePanel.vue, composables/use-explore-context.ts, .planning/phases/02-explore-inputs-and-context-layer/02-CONTEXT.md</read_first>
  <action>Replace the prototype days/interests/filter controls with design-system-aligned components. `trip-preferences.vue` should let the user choose trip duration by number of days and interests that influence the future route. `place-filters.vue` should manage reusable filters for candidate or generated places, such as interest/category search and selected chips, without requiring generated route data yet. Compose these with `city-typeahead.vue` inside `route-panel.vue`, and wire `pages/explore.vue` to the new panel/context while preserving the existing full-screen Explore shell.</action>
  <verify>
    <automated>rg --fixed-strings "ExploreCityTypeahead" components/explore/route-panel.vue</automated>
    <automated>rg --fixed-strings "ExploreTripPreferences" components/explore/route-panel.vue</automated>
    <automated>rg --fixed-strings "ExplorePlaceFilters" components/explore/route-panel.vue</automated>
    <automated>rg --fixed-strings "selectedDays" composables/use-explore-context.ts components/explore/trip-preferences.vue</automated>
  </verify>
  <acceptance_criteria>
    - User can choose trip days before generation.
    - User can choose interests before generation.
    - User can set search/filter state that later candidate/generated place views can reuse.
    - UI follows existing Tailwind/daisyUI/Nuxt icon conventions.
  </acceptance_criteria>
  <done>Explore input panel captures the Phase 2 trip preferences and filter requirements.</done>
</task>

</tasks>

<verification>
- [ ] `rg --fixed-strings "ExploreRequestContext" composables/use-explore-context.ts` passes.
- [ ] `rg --fixed-strings "/api/explore/city-suggest" components/explore/city-typeahead.vue` passes.
- [ ] `Get-ChildItem components/explore | Select-Object -ExpandProperty Name | Select-String -Pattern "^[A-Z]" -Quiet` returns false for touched Explore files.
- [ ] `npm run lint:source -- --quiet` is attempted; if existing unrelated blockers remain, record exact blockers in the summary.
</verification>

<success_criteria>

- EXPIN-01 is covered by live city typeahead UI.
- EXPIN-02 is covered by trip days controls.
- EXPIN-03 is covered by interests controls.
- EXPIN-04 is covered by reusable filter state/control surface.
- D-06 through D-09 are represented in executable tasks.

</success_criteria>

<output>
After completion, create `.planning/phases/02-explore-inputs-and-context-layer/02-02-SUMMARY.md`
</output>
