---
phase: 03-ai-route-generation-and-streaming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ai/route-contract.ts
  - lib/db/schema/ai-route.ts
  - lib/db/schema/index.ts
  - lib/db/queries/ai-route.ts
  - tests/server/ai-route-contract.test.mjs
  - tests/server/ai-route-persistence.test.mjs
autonomous: true
requirements:
  - AIROUTE-05
  - AIROUTE-06
user_setup:
  - "Schema work requires the normal project database environment. Run or have available the same Turso/local database setup used by existing Drizzle commands."
must_haves:
  truths:
    - "Generated routes are persisted as user-owned sessions and route variants, not only latest responses."
    - "The route contract contains coordinates, day grouping, timing, rationale, confidence/alternative metadata, distance hints, and optional cost metadata."
    - "All persisted route records are scoped by authenticated user ownership."
    - "Raw provider output is not trusted route data until it passes Zod validation."
    - "Covers CONTEXT decisions D-01, D-02, D-03, D-04, D-12, D-13, D-14, D-15, D-16, D-17, and D-18."
  artifacts:
    - path: "lib/ai/route-contract.ts"
      provides: "Zod schemas and TypeScript types for route events, route points, variants, and requests"
      contains: "RouteEventEnvelopeSchema"
    - path: "lib/db/schema/ai-route.ts"
      provides: "Drizzle schema for route sessions, messages, variants, points, and events"
      contains: "aiRouteSession"
    - path: "lib/db/queries/ai-route.ts"
      provides: "Ownership-safe route persistence and history queries"
      contains: "userId"
    - path: "tests/server/ai-route-contract.test.mjs"
      provides: "Focused contract tests for map-ready route event validation"
      min_lines: 20
    - path: "tests/server/ai-route-persistence.test.mjs"
      provides: "Focused source/pure-helper tests for user ownership and persistence shape"
      min_lines: 20
  key_links:
    - from: "server/api/ai/route.post.ts"
      to: "lib/ai/route-contract.ts"
      via: "streaming endpoint validates route events before emitting/persisting"
      pattern: "RouteEventEnvelopeSchema"
    - from: "lib/db/queries/ai-route.ts"
      to: "lib/db/schema/ai-route.ts"
      via: "query helpers persist sessions, variants, route points, and event logs"
      pattern: "aiRouteVariant"
---

<objective>
Create the route-generation contract and persistence foundation for Phase 3.

Purpose: The streaming endpoint and client UI need a stable internal route-event contract and user-owned route history before any provider calls are implemented.
Output: Zod route contract, Drizzle AI route tables, ownership-safe query helpers, schema export, focused tests, and a blocking schema push task.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-ai-route-generation-and-streaming/03-CONTEXT.md
@.planning/phases/03-ai-route-generation-and-streaming/03-AI-SPEC.md
@.planning/phases/03-ai-route-generation-and-streaming/03-RESEARCH.md
@.planning/phases/03-ai-route-generation-and-streaming/03-VALIDATION.md
@lib/explore/context.ts
@lib/db/schema/index.ts
@lib/db/schema/location.ts
@lib/db/schema/location-log.ts
@lib/db/queries/explore-context.ts
@tests/server/explore-context.test.mjs
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-03-01 | Invalid provider output is persisted as trusted route data. | high | Require `RouteEventEnvelopeSchema.safeParse` before persistence and tests for invalid coordinates/day/timing. |
| T-03-02 | User can read or mutate another user's route session or variant. | high | Query helpers take `userId` explicitly and filter every route-session read/write by authenticated ownership. |
| T-03-03 | Schema files change but database schema is not pushed, creating false-positive verification. | high | Include blocking Drizzle push task after schema edits. |
| T-03-04 | Cost/price fields look authoritative without source/confidence. | medium | Make price optional and require confidence/source metadata when present. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Define the internal route event contract</name>
  <files>lib/ai/route-contract.ts</files>
  <read_first>.planning/phases/03-ai-route-generation-and-streaming/03-CONTEXT.md, .planning/phases/03-ai-route-generation-and-streaming/03-AI-SPEC.md, lib/explore/context.ts</read_first>
  <action>Create `lib/ai/route-contract.ts` with Zod schemas and exported TypeScript types. Include `RouteGenerationRequestSchema` with `context`, optional `sessionId`, optional `activeVariantId`, and optional `followUpMessage`. Include `RoutePointSchema` with `id`, `name`, `day`, `coordinates.lat`, `coordinates.long`, `estimatedStart`, `estimatedDurationMinutes`, `rationale`, `confidence`, `alternativeForPointId`, `approximateDistanceMeters`, `estimatedPriceLevel`, and `priceConfidence`. Include `RouteEventEnvelopeSchema` with allowlisted event types: `route.session.created`, `route.variant.started`, `route.point.added`, `route.point.updated`, `route.variant.completed`, `route.warning`, and `route.failed`. Export helper functions `isRoutePointEvent` and `createRouteWarningEvent`. Do not import Vue refs, browser APIs, provider SDKs, or database modules.</action>
  <verify>
    <automated>Test-Path lib/ai/route-contract.ts</automated>
    <automated>rg --fixed-strings "RouteGenerationRequestSchema" lib/ai/route-contract.ts</automated>
    <automated>rg --fixed-strings "RouteEventEnvelopeSchema" lib/ai/route-contract.ts</automated>
    <automated>rg --fixed-strings "estimatedPriceLevel" lib/ai/route-contract.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/ai/route-contract.ts` exports `RouteGenerationRequestSchema`.
    - `lib/ai/route-contract.ts` exports `RoutePointSchema`.
    - `lib/ai/route-contract.ts` exports `RouteEventEnvelopeSchema`.
    - `RoutePointSchema` validates coordinates, day grouping, timing, rationale, confidence, distance, and optional price metadata.
    - The file contains no `ref(`, `computed(`, `fetch(`, or `process.env`.
  </acceptance_criteria>
  <done>Route events and points have a typed internal contract for later endpoint and UI work.</done>
</task>

<task type="auto">
  <name>Task 2: Add Drizzle route history schema</name>
  <files>lib/db/schema/ai-route.ts, lib/db/schema/index.ts</files>
  <read_first>lib/db/schema/index.ts, lib/db/schema/auth.ts, lib/db/schema/location.ts, lib/db/schema/location-log.ts, .planning/phases/03-ai-route-generation-and-streaming/03-AI-SPEC.md</read_first>
  <action>Create `lib/db/schema/ai-route.ts` with Drizzle sqlite tables named `aiRouteSession`, `aiRouteMessage`, `aiRouteVariant`, `aiRoutePoint`, and `aiRouteEvent`. Every table must either include a non-null `userId` referencing `user.id` or be linked to a parent that includes `userId`; prefer including `userId` on all five tables for simple ownership filters. `aiRouteSession` stores status, city name/provider id, request context JSON text, active variant id, createdAt, and updateAt. `aiRouteMessage` stores session id, role (`user`, `assistant`, `system`), safe summary text, and createdAt. `aiRouteVariant` stores session id, parent variant id, status (`generating`, `completed`, `failed`), title, summary, failureCode, createdAt, and updateAt. `aiRoutePoint` stores variant id, sequence, day, name, lat, long, estimatedStart, estimatedDurationMinutes, rationale, confidence, alternativeForPointId, approximateDistanceMeters, estimatedPriceLevel, priceConfidence, and raw source metadata JSON text only if sanitized. `aiRouteEvent` stores session id, variant id, sequence, type, payload JSON text, validationStatus, and createdAt. Export all new tables and relations from `lib/db/schema/index.ts`.</action>
  <verify>
    <automated>rg --fixed-strings "aiRouteSession" lib/db/schema/ai-route.ts lib/db/schema/index.ts</automated>
    <automated>rg --fixed-strings "aiRouteVariant" lib/db/schema/ai-route.ts</automated>
    <automated>rg --fixed-strings "aiRoutePoint" lib/db/schema/ai-route.ts</automated>
    <automated>rg --fixed-strings "userId" lib/db/schema/ai-route.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/db/schema/ai-route.ts` defines all five tables: `aiRouteSession`, `aiRouteMessage`, `aiRouteVariant`, `aiRoutePoint`, `aiRouteEvent`.
    - `lib/db/schema/ai-route.ts` contains `userId` for ownership filtering.
    - `lib/db/schema/index.ts` exports from `"./ai-route"`.
    - `aiRoutePoint` contains `lat`, `long`, `day`, `rationale`, `confidence`, and `approximateDistanceMeters`.
  </acceptance_criteria>
  <done>Route sessions, messages, variants, points, and events have schema support.</done>
</task>

<task type="auto">
  <name>Task 3: Add ownership-safe route query helpers</name>
  <files>lib/db/queries/ai-route.ts</files>
  <read_first>lib/db/queries/explore-context.ts, lib/db/queries/location.ts, lib/db/schema/ai-route.ts, lib/ai/route-contract.ts</read_first>
  <action>Create `lib/db/queries/ai-route.ts` with query helpers that all accept `userId: number`. Include `createAiRouteSession`, `appendAiRouteMessage`, `createAiRouteVariant`, `persistAiRoutePoint`, `persistAiRouteEvent`, `markAiRouteVariantCompleted`, `markAiRouteVariantFailed`, `findAiRouteSessionsByUserId`, and `findAiRouteSessionByIdForUser`. Each read must filter by `userId`; each write must set `userId` from the authenticated caller, never from the browser body. Keep helper inputs typed from `lib/ai/route-contract.ts` where practical. Do not log prompts, raw provider headers, `.env`, or full personal context.</action>
  <verify>
    <automated>Test-Path lib/db/queries/ai-route.ts</automated>
    <automated>rg --fixed-strings "findAiRouteSessionByIdForUser" lib/db/queries/ai-route.ts</automated>
    <automated>rg --fixed-strings "userId" lib/db/queries/ai-route.ts</automated>
    <automated>if (Select-String -Path lib/db/queries/ai-route.ts -Pattern "console\\." -Quiet) { throw "console logging found" }</automated>
  </verify>
  <acceptance_criteria>
    - Query helper names listed in the action exist.
    - Every exported read helper accepts or uses `userId`.
    - Browser-supplied `userId` is not accepted in helper input types.
    - The file contains no `console.log`, `console.error`, `.env`, or provider header strings.
  </acceptance_criteria>
  <done>Persistence helpers enforce route history ownership before endpoints exist.</done>
</task>

<task type="auto">
  <name>Task 4: Add route contract and persistence tests</name>
  <files>tests/server/ai-route-contract.test.mjs, tests/server/ai-route-persistence.test.mjs</files>
  <read_first>scripts/run-node-tests.mjs, tests/server/explore-context.test.mjs, tests/server/explore-search.test.mjs, lib/ai/route-contract.ts, lib/db/queries/ai-route.ts</read_first>
  <action>Add `tests/server/ai-route-contract.test.mjs` and `tests/server/ai-route-persistence.test.mjs` using Node's built-in `node:test` and `node:assert/strict`. Tests may inspect source text and pure helper exports; do not call real OpenAI, Turso, network, or `.env`. Contract tests must assert route point schemas reject invalid coordinates and accept optional cost only with confidence metadata. Persistence/source tests must assert query helpers include `userId`, expected helper names, no `console.`, and no `process.env` access.</action>
  <verify>
    <automated>node scripts/run-node-tests.mjs tests/server/ai-route-contract.test.mjs tests/server/ai-route-persistence.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `tests/server/ai-route-contract.test.mjs` contains `RouteEventEnvelopeSchema`.
    - `tests/server/ai-route-persistence.test.mjs` contains `findAiRouteSessionByIdForUser`.
    - Test command exits 0 without provider credentials or network.
  </acceptance_criteria>
  <done>Route contract and ownership helpers have focused regression tests.</done>
</task>

<task type="auto">
  <name>Task 5: [BLOCKING] Push Drizzle schema after AI route schema edits</name>
  <files>lib/db/schema/ai-route.ts, drizzle.config.ts</files>
  <read_first>drizzle.config.ts, lib/db/schema/ai-route.ts, package.json</read_first>
  <action>After Tasks 1-4 are complete and schema files are saved, run the Drizzle schema push command `pnpm exec drizzle-kit push`. If the command requires unavailable database credentials or an interactive prompt that cannot be handled safely, stop execution and record the exact blocker in `03-01-SUMMARY.md`; do not claim Phase 3 schema verification is complete without naming the blocker. Do not run destructive database commands outside the configured Drizzle path.</action>
  <verify>
    <automated>pnpm exec drizzle-kit push</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec drizzle-kit push` exits 0, or `03-01-SUMMARY.md` records the exact credential/interactive blocker.
    - This task runs after `lib/db/schema/ai-route.ts` exists.
  </acceptance_criteria>
  <done>Database schema state is not falsely assumed after Drizzle schema changes.</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/run-node-tests.mjs tests/server/ai-route-contract.test.mjs tests/server/ai-route-persistence.test.mjs` passes.
- [ ] `rg --fixed-strings "RouteEventEnvelopeSchema" lib/ai/route-contract.ts` passes.
- [ ] `rg --fixed-strings "aiRouteSession" lib/db/schema/ai-route.ts lib/db/schema/index.ts` passes.
- [ ] `rg --fixed-strings "findAiRouteSessionByIdForUser" lib/db/queries/ai-route.ts` passes.
- [ ] `pnpm exec drizzle-kit push` passes or the exact blocker is recorded.
</verification>

<success_criteria>

- AIROUTE-05 is covered by user-owned route sessions, messages, variants, points, and query helpers.
- AIROUTE-06 is covered by map-ready route point/event schemas.
- CONTEXT decisions D-01 through D-04 and D-12 through D-18 are represented in executable tasks.
- AI-SPEC guardrails for validation-before-persistence and optional cost metadata are reflected in code tasks.

</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-route-generation-and-streaming/03-01-SUMMARY.md`
</output>

