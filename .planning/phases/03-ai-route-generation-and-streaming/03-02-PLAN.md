---
phase: 03-ai-route-generation-and-streaming
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01-PLAN.md
files_modified:
  - lib/ai/openai-compatible.ts
  - lib/ai/route-prompts.ts
  - lib/ai/route-context.ts
  - server/api/ai/route.post.ts
  - lib/env.ts
  - tests/server/ai-route-stream.test.mjs
  - tests/server/ai-route-context.test.mjs
autonomous: true
requirements:
  - AIROUTE-01
  - AIROUTE-02
  - AIROUTE-03
  - AIROUTE-05
  - AIROUTE-06
user_setup:
  - "Real provider calls require server-only OPENAI_API_KEY and OPENAI_ROUTE_MODEL configuration. Tests must not require these credentials."
must_haves:
  truths:
    - "The route-generation endpoint is authenticated and validates the request before provider calls."
    - "Provider credentials stay server-only and are never exposed through public runtime config."
    - "Streaming emits app-owned route events, not raw provider chunks or raw JSON UI."
    - "Selected sidebar context is re-read and summarized server-side by authenticated user ownership."
    - "Covers CONTEXT decisions D-05, D-06, D-07, D-09, D-10, and D-11."
  artifacts:
    - path: "server/api/ai/route.post.ts"
      provides: "Authenticated streaming route-generation endpoint"
      contains: "defineAuthenticatedHandler"
    - path: "lib/ai/openai-compatible.ts"
      provides: "Server-only OpenAI-compatible streaming provider client"
      contains: "fetchOpenAiCompatibleRouteStream"
    - path: "lib/ai/route-context.ts"
      provides: "Bounded selected-context summarizer"
      contains: "buildSelectedRouteContext"
    - path: "lib/ai/route-prompts.ts"
      provides: "Server-owned route-generation prompt instructions"
      contains: "ROUTE_SYSTEM_INSTRUCTIONS"
    - path: "tests/server/ai-route-stream.test.mjs"
      provides: "Focused streaming/provider source and parser tests"
      min_lines: 20
  key_links:
    - from: "server/api/ai/route.post.ts"
      to: "lib/db/queries/ai-route.ts"
      via: "endpoint creates sessions/variants and persists validated route events"
      pattern: "createAiRouteSession"
    - from: "server/api/ai/route.post.ts"
      to: "lib/ai/route-context.ts"
      via: "endpoint summarizes selected Phase 2 context before provider call"
      pattern: "buildSelectedRouteContext"
---

<objective>
Implement the authenticated AI route streaming endpoint and provider pipeline.

Purpose: Users need to request AI route generation and receive progressive route events while the server validates, persists, and scopes all AI work.
Output: server-only OpenAI-compatible client, prompt/context assembly, authenticated streaming endpoint, provider/request env config, and focused tests without real provider calls.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-ai-route-generation-and-streaming/03-CONTEXT.md
@.planning/phases/03-ai-route-generation-and-streaming/03-AI-SPEC.md
@.planning/phases/03-ai-route-generation-and-streaming/03-RESEARCH.md
@.planning/phases/03-ai-route-generation-and-streaming/03-01-PLAN.md
@lib/ai/route-contract.ts
@lib/db/queries/ai-route.ts
@lib/db/queries/explore-context.ts
@lib/explore/context.ts
@utils/define-authenticated-handler.ts
@lib/env.ts
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-03-05 | Provider credentials are exposed to browser runtime or logs. | high | Read provider config only from server `lib/env.ts`; never put OpenAI keys in public runtime config; tests grep for public exposure. |
| T-03-06 | Unselected saved places/diary logs are sent to AI provider. | high | `buildSelectedRouteContext` re-reads selected ids by authenticated `userId` and caps fields. |
| T-03-07 | Raw provider chunks or malformed JSON are streamed to UI. | high | Normalize provider chunks into allowlisted `RouteEventEnvelopeSchema` events before emitting. |
| T-03-08 | Provider failure leaks prompt or header details. | medium | Return safe route warning/failed events and sanitized error codes only. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add server-only OpenAI-compatible stream client</name>
  <files>lib/ai/openai-compatible.ts, lib/env.ts</files>
  <read_first>.planning/phases/03-ai-route-generation-and-streaming/03-AI-SPEC.md, lib/env.ts, server/api/explore/candidate-places.get.ts</read_first>
  <action>Add `OPENAI_API_KEY`, optional `OPENAI_BASE_URL`, and `OPENAI_ROUTE_MODEL` to the safe server env schema in `lib/env.ts`. Create `lib/ai/openai-compatible.ts` with `fetchOpenAiCompatibleRouteStream`, `parseOpenAiSseLines`, and `sanitizeProviderError`. Use native `fetch` against `${OPENAI_BASE_URL || "https://api.openai.com/v1"}/responses`, pass `Authorization: Bearer ${OPENAI_API_KEY}`, request streaming output, and return parsed provider events through async iteration. Keep this file server-only by not importing it from components/composables. Do not log request body, headers, prompts, or raw selected context.</action>
  <verify>
    <automated>rg --fixed-strings "OPENAI_API_KEY" lib/env.ts lib/ai/openai-compatible.ts</automated>
    <automated>rg --fixed-strings "fetchOpenAiCompatibleRouteStream" lib/ai/openai-compatible.ts</automated>
    <automated>rg --fixed-strings "/responses" lib/ai/openai-compatible.ts</automated>
    <automated>if (Select-String -Path lib/ai/openai-compatible.ts -Pattern "console\\." -Quiet) { throw "console logging found" }</automated>
  </verify>
  <acceptance_criteria>
    - `lib/env.ts` contains `OPENAI_API_KEY` and `OPENAI_ROUTE_MODEL`.
    - `lib/ai/openai-compatible.ts` contains `fetchOpenAiCompatibleRouteStream`.
    - Provider base URL is server-side and defaults to `https://api.openai.com/v1`.
    - The file contains no `runtimeConfig.public`, `console.log`, or provider header logging.
  </acceptance_criteria>
  <done>Server code can initiate OpenAI-compatible streaming without exposing provider config.</done>
</task>

<task type="auto">
  <name>Task 2: Add route prompt and selected-context summarizer</name>
  <files>lib/ai/route-prompts.ts, lib/ai/route-context.ts</files>
  <read_first>lib/explore/context.ts, lib/db/queries/explore-context.ts, .planning/phases/03-ai-route-generation-and-streaming/03-CONTEXT.md, .planning/phases/03-ai-route-generation-and-streaming/03-AI-SPEC.md</read_first>
  <action>Create `lib/ai/route-prompts.ts` with `ROUTE_SYSTEM_INSTRUCTIONS` and `buildRouteGenerationInput`. The instructions must explicitly say: output app route events only, raw JSON is not user-facing UI, use only selected sidebar context, make price optional with confidence, include coordinates/day/timing/rationale/confidence/distance where possible, and preserve route variants on follow-up. Create `lib/ai/route-context.ts` with `buildSelectedRouteContext(userId, requestContext)` that re-reads user-owned saved places and diary logs from existing query helpers, filters them by `selectedSavedPlaceIds` and `selectedDiaryLogIds`, caps selected saved places to 10, diary logs to 10, candidate places to 12, and truncates descriptions to 240 characters. Do not pass full diary payloads or images.</action>
  <verify>
    <automated>rg --fixed-strings "ROUTE_SYSTEM_INSTRUCTIONS" lib/ai/route-prompts.ts</automated>
    <automated>rg --fixed-strings "buildSelectedRouteContext" lib/ai/route-context.ts</automated>
    <automated>rg --fixed-strings "selectedSavedPlaceIds" lib/ai/route-context.ts</automated>
    <automated>rg --fixed-strings "240" lib/ai/route-context.ts</automated>
  </verify>
  <acceptance_criteria>
    - Prompt instructions include the exact phrase `raw JSON is not user-facing UI`.
    - `buildSelectedRouteContext` accepts `userId`.
    - `buildSelectedRouteContext` filters by `selectedSavedPlaceIds` and `selectedDiaryLogIds`.
    - Context summarization caps or truncates selected context before provider input.
  </acceptance_criteria>
  <done>Provider prompts use bounded user-selected context and locked route-event behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add authenticated streaming route endpoint</name>
  <files>server/api/ai/route.post.ts</files>
  <read_first>utils/define-authenticated-handler.ts, lib/ai/route-contract.ts, lib/ai/openai-compatible.ts, lib/ai/route-context.ts, lib/ai/route-prompts.ts, lib/db/queries/ai-route.ts</read_first>
  <action>Create `server/api/ai/route.post.ts` as an authenticated POST endpoint. It must use `defineAuthenticatedHandler`, validate the body with `RouteGenerationRequestSchema`, use `event.context.user.id`, call `buildSelectedRouteContext`, create or load a user-owned route session, create a new route variant for generation/follow-up, stream allowlisted app events, validate each event with `RouteEventEnvelopeSchema`, persist validated events/points through `lib/db/queries/ai-route.ts`, and emit safe warnings for skipped invalid chunks. The endpoint must not render raw JSON as UI; it returns an event stream suitable for client code. If Nitro stream helper choice is uncertain, use a local `ReadableStream`/response pattern compatible with Nuxt server routes and document any fallback in the summary.</action>
  <verify>
    <automated>Test-Path server/api/ai/route.post.ts</automated>
    <automated>rg --fixed-strings "defineAuthenticatedHandler" server/api/ai/route.post.ts</automated>
    <automated>rg --fixed-strings "RouteGenerationRequestSchema" server/api/ai/route.post.ts</automated>
    <automated>rg --fixed-strings "RouteEventEnvelopeSchema" server/api/ai/route.post.ts</automated>
    <automated>rg --fixed-strings "event.context.user.id" server/api/ai/route.post.ts</automated>
  </verify>
  <acceptance_criteria>
    - Endpoint path is `server/api/ai/route.post.ts`.
    - Endpoint is authenticated.
    - Endpoint validates request body before provider calls.
    - Endpoint validates route events before persistence/emission.
    - Endpoint persists route session/variant data for user-owned history.
  </acceptance_criteria>
  <done>Explore has an authenticated route-generation streaming API.</done>
</task>

<task type="auto">
  <name>Task 4: Add streaming and selected-context tests</name>
  <files>tests/server/ai-route-stream.test.mjs, tests/server/ai-route-context.test.mjs</files>
  <read_first>scripts/run-node-tests.mjs, tests/server/explore-context.test.mjs, lib/ai/openai-compatible.ts, lib/ai/route-context.ts, server/api/ai/route.post.ts</read_first>
  <action>Add tests using `node:test` and `node:assert/strict`. `ai-route-stream.test.mjs` should use fake SSE/chunk strings or source inspection to verify provider parser handles multiple events, ignores done markers, and exposes `fetchOpenAiCompatibleRouteStream` without logging secrets. `ai-route-context.test.mjs` should verify selected-context summarization references `selectedSavedPlaceIds`, `selectedDiaryLogIds`, caps/truncation, and `userId`. Tests must not call real provider APIs, network, Turso, or `.env`.</action>
  <verify>
    <automated>node scripts/run-node-tests.mjs tests/server/ai-route-stream.test.mjs tests/server/ai-route-context.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Streaming tests do not require `OPENAI_API_KEY`.
    - Context tests assert selected context ids are used.
    - Tests fail if provider/client files contain `console.log` or `runtimeConfig.public` for OpenAI config.
  </acceptance_criteria>
  <done>Streaming and selected-context behavior has focused test coverage without real provider calls.</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/run-node-tests.mjs tests/server/ai-route-stream.test.mjs tests/server/ai-route-context.test.mjs` passes.
- [ ] `rg --fixed-strings "defineAuthenticatedHandler" server/api/ai/route.post.ts` passes.
- [ ] `rg --fixed-strings "OPENAI_API_KEY" lib/env.ts lib/ai/openai-compatible.ts` passes.
- [ ] `rg --fixed-strings "raw JSON is not user-facing UI" lib/ai/route-prompts.ts` passes.
- [ ] No real provider call is required for tests.
</verification>

<success_criteria>

- AIROUTE-01 is covered by authenticated route-generation request handling.
- AIROUTE-02 is covered by route-event streaming.
- AIROUTE-03 is covered by bounded selected context summarization.
- AIROUTE-05 is covered by session/variant persistence integration.
- AIROUTE-06 is covered by validated structured route events.
- CONTEXT decisions D-05 through D-11 are represented in executable tasks.

</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-route-generation-and-streaming/03-02-SUMMARY.md`
</output>

