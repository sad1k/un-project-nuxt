---
phase: 03-ai-route-generation-and-streaming
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01-PLAN.md
  - 03-02-PLAN.md
files_modified:
  - composables/use-ai-route-session.ts
  - composables/use-route-generator.ts
  - components/explore/route-panel.vue
  - components/explore/route-history.vue
  - components/explore/route-follow-up.vue
  - pages/explore.vue
  - tests/server/ai-route-client.test.mjs
  - .planning/phases/03-ai-route-generation-and-streaming/03-HANDOFF.md
autonomous: true
requirements:
  - AIROUTE-01
  - AIROUTE-02
  - AIROUTE-04
  - AIROUTE-05
  - AIROUTE-06
user_setup:
  - "Manual browser verification should confirm no raw JSON is visible and route variants can be switched after follow-up."
must_haves:
  truths:
    - "Explore submits the typed Phase 2 request context to the Phase 3 route stream instead of using the old mock generator as the primary path."
    - "The primary user experience is route progress/state and eventually map-ready points, not a chat transcript or visible raw JSON."
    - "Follow-up input exists for changing wishes, replacing route points, or refining the active route."
    - "Generated route variants remain switchable within the active route session."
    - "Phase 4 receives a handoff describing route event fields and map-rendering responsibilities."
  artifacts:
    - path: "composables/use-ai-route-session.ts"
      provides: "Client route stream state, active session, route variants, and follow-up submission"
      contains: "activeVariantId"
    - path: "components/explore/route-history.vue"
      provides: "Variant/history switcher for generated routes"
      contains: "variant"
    - path: "components/explore/route-follow-up.vue"
      provides: "Follow-up refinement input"
      contains: "followUpMessage"
    - path: ".planning/phases/03-ai-route-generation-and-streaming/03-HANDOFF.md"
      provides: "Phase 4 map-rendering handoff"
      contains: "RouteEventEnvelope"
    - path: "tests/server/ai-route-client.test.mjs"
      provides: "Focused source checks for client route stream integration"
      min_lines: 20
  key_links:
    - from: "composables/use-ai-route-session.ts"
      to: "server/api/ai/route.post.ts"
      via: "client composable submits route context and consumes route events"
      pattern: "/api/ai/route"
    - from: "components/explore/route-history.vue"
      to: "composables/use-ai-route-session.ts"
      via: "history UI switches active route variants"
      pattern: "activeVariantId"
---

<objective>
Connect Explore to the AI route stream, route variant history, and follow-up refinement UX.

Purpose: Phase 3 must make route generation usable from Explore without turning the app into a chat-first UI or taking over Phase 4's full map rendering work.
Output: client composable for route sessions, progressive route-event state, history switcher, follow-up input, route panel integration, tests, and Phase 4 handoff.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-ai-route-generation-and-streaming/03-CONTEXT.md
@.planning/phases/03-ai-route-generation-and-streaming/03-AI-SPEC.md
@.planning/phases/03-ai-route-generation-and-streaming/03-RESEARCH.md
@.planning/phases/03-ai-route-generation-and-streaming/03-01-PLAN.md
@.planning/phases/03-ai-route-generation-and-streaming/03-02-PLAN.md
@lib/ai/route-contract.ts
@composables/use-explore-context.ts
@composables/use-route-generator.ts
@components/explore/route-panel.vue
@pages/explore.vue
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-03-09 | Client renders raw event JSON to user. | high | UI components render selected fields only: names, short rationale/status, variant labels, and route progress. |
| T-03-10 | Follow-up request submits stale or wrong variant id. | medium | `use-ai-route-session.ts` tracks `sessionId` and `activeVariantId`; follow-up payload includes both. |
| T-03-11 | Mock route generator remains the primary path and bypasses persistence. | medium | Replace primary generate action with `/api/ai/route`; keep mock only as isolated fixture/fallback if clearly named. |
| T-03-12 | Phase 4 lacks enough handoff detail to render route events. | medium | Write `03-HANDOFF.md` with event fields, route point fields, deferred map responsibilities, and manual checks. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add client route session stream composable</name>
  <files>composables/use-ai-route-session.ts</files>
  <read_first>lib/ai/route-contract.ts, composables/use-explore-context.ts, composables/use-route-generator.ts, .planning/phases/03-ai-route-generation-and-streaming/03-CONTEXT.md</read_first>
  <action>Create `composables/use-ai-route-session.ts` with refs for `sessionId`, `activeVariantId`, `variants`, `events`, `pointsByVariantId`, `isGenerating`, `error`, and `lastWarning`. Export `generateRoute(requestContext)`, `submitFollowUp(followUpMessage)`, `setActiveVariant(variantId)`, and `resetRouteSession`. `generateRoute` posts to `/api/ai/route` using the computed `ExploreRequestContext`, consumes streamed app route events, appends route points by `variantId`, and never stores raw provider chunks for display. `submitFollowUp` must include current `sessionId`, current `activeVariantId`, and `followUpMessage`. Do not call provider APIs directly from the browser.</action>
  <verify>
    <automated>Test-Path composables/use-ai-route-session.ts</automated>
    <automated>rg --fixed-strings "/api/ai/route" composables/use-ai-route-session.ts</automated>
    <automated>rg --fixed-strings "activeVariantId" composables/use-ai-route-session.ts</automated>
    <automated>rg --fixed-strings "submitFollowUp" composables/use-ai-route-session.ts</automated>
  </verify>
  <acceptance_criteria>
    - Composable exports `generateRoute`, `submitFollowUp`, and `setActiveVariant`.
    - Composable tracks `activeVariantId`.
    - Composable posts to `/api/ai/route`.
    - Composable contains no `OPENAI_API_KEY`, `OPENAI_ROUTE_MODEL`, or provider base URL.
  </acceptance_criteria>
  <done>Explore client has state for route streaming and variant switching.</done>
</task>

<task type="auto">
  <name>Task 2: Replace mock generation primary path with AI route stream</name>
  <files>composables/use-route-generator.ts, components/explore/route-panel.vue, pages/explore.vue</files>
  <read_first>composables/use-route-generator.ts, composables/use-ai-route-session.ts, composables/use-explore-context.ts, components/explore/route-panel.vue, pages/explore.vue</read_first>
  <action>Update Explore generation wiring so the primary generate action uses `useAiRouteSession().generateRoute(requestContext.value)` instead of the old hard-coded Tokyo/Paris/fallback mock. If `use-route-generator.ts` is retained, rename its public role to a fixture/fallback helper and ensure route panel imports the AI route session composable for real generation. Keep Phase 4 map animation out of scope: it is acceptable for Phase 3 to expose route points/state that the current prototype or a simple route-progress area can display. Preserve `ExploreRequestContext` as the request boundary.</action>
  <verify>
    <automated>rg --fixed-strings "useAiRouteSession" components/explore/route-panel.vue pages/explore.vue composables/use-route-generator.ts</automated>
    <automated>rg --fixed-strings "generateRoute" components/explore/route-panel.vue pages/explore.vue</automated>
    <automated>rg --fixed-strings "ExploreRequestContext" composables/use-explore-context.ts</automated>
  </verify>
  <acceptance_criteria>
    - Real generation path calls `useAiRouteSession`.
    - Route generation request uses `ExploreRequestContext`.
    - Hard-coded route generator is not the main generate path.
    - No full Phase 4 map route-line/day-selection implementation is added in this task.
  </acceptance_criteria>
  <done>Explore uses the Phase 3 route stream as its primary generation path.</done>
</task>

<task type="auto">
  <name>Task 3: Add route history and follow-up components</name>
  <files>components/explore/route-history.vue, components/explore/route-follow-up.vue, components/explore/route-panel.vue</files>
  <read_first>composables/use-ai-route-session.ts, components/explore/route-panel.vue, .planning/phases/03-ai-route-generation-and-streaming/03-CONTEXT.md</read_first>
  <action>Create `components/explore/route-history.vue` to display generated route variants and allow `setActiveVariant(variantId)` switching. Create `components/explore/route-follow-up.vue` with a compact follow-up input for changed wishes, replacing a point, or refining the active route; it should call `submitFollowUp(followUpMessage)`. Compose both into `route-panel.vue` only when a route session exists or generation is in progress. Render user-facing fields such as variant title/status, point count, place name, short rationale, and warning messages; never render `JSON.stringify(event)` or raw provider chunks.</action>
  <verify>
    <automated>rg --fixed-strings "setActiveVariant" components/explore/route-history.vue</automated>
    <automated>rg --fixed-strings "submitFollowUp" components/explore/route-follow-up.vue</automated>
    <automated>rg --fixed-strings "followUpMessage" components/explore/route-follow-up.vue</automated>
    <automated>if (Select-String -Path components/explore/route-history.vue,components/explore/route-follow-up.vue,components/explore/route-panel.vue -Pattern "JSON\\.stringify" -Quiet) { throw "raw JSON rendering found" }</automated>
  </verify>
  <acceptance_criteria>
    - Route history UI can switch active variants.
    - Follow-up UI can submit a non-empty message for the active session/variant.
    - Components do not render raw JSON.
    - Chat/follow-up UI is secondary to route generation state, not the primary first-run surface.
  </acceptance_criteria>
  <done>Users can refine routes and switch variants without a chat-first UI.</done>
</task>

<task type="auto">
  <name>Task 4: Add client integration tests and Phase 4 handoff</name>
  <files>tests/server/ai-route-client.test.mjs, .planning/phases/03-ai-route-generation-and-streaming/03-HANDOFF.md</files>
  <read_first>tests/server/explore-context.test.mjs, composables/use-ai-route-session.ts, components/explore/route-history.vue, components/explore/route-follow-up.vue, lib/ai/route-contract.ts, .planning/phases/03-ai-route-generation-and-streaming/03-AI-SPEC.md</read_first>
  <action>Add `tests/server/ai-route-client.test.mjs` with source-level tests that assert `/api/ai/route`, `activeVariantId`, `submitFollowUp`, `setActiveVariant`, and no `JSON.stringify` raw event rendering in the touched Explore route components. Create `03-HANDOFF.md` documenting the route event envelope, route point fields, how Phase 4 should consume points for markers/lines/day grouping/animation, what Phase 3 deliberately did not render, and manual browser checks for no raw JSON UI and route variant switching.</action>
  <verify>
    <automated>node scripts/run-node-tests.mjs tests/server/ai-route-client.test.mjs</automated>
    <automated>rg --fixed-strings "RouteEventEnvelope" .planning/phases/03-ai-route-generation-and-streaming/03-HANDOFF.md</automated>
    <automated>rg --fixed-strings "Phase 4" .planning/phases/03-ai-route-generation-and-streaming/03-HANDOFF.md</automated>
    <automated>rg --fixed-strings "raw JSON" .planning/phases/03-ai-route-generation-and-streaming/03-HANDOFF.md</automated>
  </verify>
  <acceptance_criteria>
    - Client integration test file exists and runs with `node:test`.
    - Handoff names every route point field Phase 4 needs.
    - Handoff explicitly states full map markers, route lines, day selection, and map animation remain Phase 4.
    - Handoff records manual check instructions for no raw JSON UI and variant switching.
  </acceptance_criteria>
  <done>Phase 3 ends with client integration checks and a clear map-rendering handoff.</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/run-node-tests.mjs tests/server/ai-route-client.test.mjs` passes.
- [ ] `rg --fixed-strings "/api/ai/route" composables/use-ai-route-session.ts` passes.
- [ ] `rg --fixed-strings "setActiveVariant" components/explore/route-history.vue` passes.
- [ ] `rg --fixed-strings "submitFollowUp" components/explore/route-follow-up.vue` passes.
- [ ] `rg --fixed-strings "RouteEventEnvelope" .planning/phases/03-ai-route-generation-and-streaming/03-HANDOFF.md` passes.
- [ ] Manual browser check records that raw JSON is not visible and route variants can be switched.
</verification>

<success_criteria>

- AIROUTE-01 is covered by Explore submitting route-generation requests.
- AIROUTE-02 is covered by client consumption of progressive route events without raw JSON UI.
- AIROUTE-04 is covered by follow-up refinement UI.
- AIROUTE-05 is covered by route session/variant history integration.
- AIROUTE-06 is covered by Phase 4 handoff and typed route point consumption.
- CONTEXT decisions D-01 through D-08 are represented in executable client tasks.

</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-route-generation-and-streaming/03-03-SUMMARY.md`
</output>

