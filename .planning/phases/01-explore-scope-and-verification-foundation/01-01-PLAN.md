---
phase: 01-explore-scope-and-verification-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - eslint.config.mjs
  - scripts/run-node-tests.mjs
  - tests/server/README.md
  - tests/server/.gitkeep
  - .planning/phases/01-explore-scope-and-verification-foundation/01-VERIFICATION-BASELINE.md
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
user_setup: []
must_haves:
  truths:
    - "Developer can run a scoped source lint command without .planning, .omx, or AGENTS.md being linted."
    - "Developer can run a focused server/data test command even before feature tests are added."
    - "Current lint and typecheck blockers relevant to Explore work are captured in a phase-local baseline."
  artifacts:
    - path: "package.json"
      provides: "Focused verification scripts"
      contains: "verify:explore-foundation"
    - path: "scripts/run-node-tests.mjs"
      provides: "Dependency-free Node test runner wrapper"
      min_lines: 30
    - path: "tests/server/README.md"
      provides: "Server/data test pattern documentation"
      min_lines: 15
    - path: ".planning/phases/01-explore-scope-and-verification-foundation/01-VERIFICATION-BASELINE.md"
      provides: "Recorded lint/typecheck blocker baseline"
      contains: "components/explore"
  key_links:
    - from: "package.json"
      to: "scripts/run-node-tests.mjs"
      via: "test:server script invokes the runner"
      pattern: "test:server.*run-node-tests"
    - from: "package.json"
      to: "eslint.config.mjs"
      via: "lint scripts use the eslint config artifact excludes"
      pattern: "lint:source"
---

<objective>
Establish the Phase 1 verification foundation for future Explore and AI work.

Purpose: Later implementation phases need runnable, scoped checks even while the historical repository still has global lint/typecheck blockers.
Output: Focused verification scripts, a dependency-free server test harness, and a baseline document listing current blockers.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-explore-scope-and-verification-foundation/01-CONTEXT.md
@.planning/phases/01-explore-scope-and-verification-foundation/01-RESEARCH.md
@package.json
@eslint.config.mjs
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scoped verification scripts</name>
  <files>package.json, eslint.config.mjs</files>
  <read_first>package.json, eslint.config.mjs, .planning/phases/01-explore-scope-and-verification-foundation/01-RESEARCH.md</read_first>
  <action>Update `package.json` scripts to add `lint:source`, `test:server`, and `verify:explore-foundation`. Keep existing `lint`, `lint:fix`, `typecheck`, and package manager fields intact. `lint:source` should run ESLint through the project config. `test:server` should call `node scripts/run-node-tests.mjs tests/server`. `verify:explore-foundation` should run the scoped source lint command and the focused server test command. Review `eslint.config.mjs` and preserve explicit ignores for `.omx/**`, `.planning/**`, `AGENTS.md`, migrations, public assets, lockfiles, generated output, and dependency folders so FOUND-01 stays true. Do not add dependencies.</action>
  <verify>
    <automated>node -e "const pkg=require('./package.json'); for (const key of ['lint:source','test:server','verify:explore-foundation']) { if (!pkg.scripts?.[key]) throw new Error('missing '+key) }"</automated>
    <automated>rg --fixed-strings '".planning/**"' eslint.config.mjs; rg --fixed-strings '".omx/**"' eslint.config.mjs; rg --fixed-strings '"AGENTS.md"' eslint.config.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains scripts named `lint:source`, `test:server`, and `verify:explore-foundation`.
    - `eslint.config.mjs` still ignores `.planning/**`, `.omx/**`, and `AGENTS.md`.
    - No new dependency or devDependency is added.
  </acceptance_criteria>
  <done>Focused verification scripts exist and generated workflow artifacts remain excluded from lint.</done>
</task>

<task type="auto">
  <name>Task 2: Create dependency-free server test harness</name>
  <files>scripts/run-node-tests.mjs, tests/server/README.md, tests/server/.gitkeep</files>
  <read_first>package.json, .planning/codebase/TESTING.md, .planning/phases/01-explore-scope-and-verification-foundation/01-RESEARCH.md</read_first>
  <action>Create `scripts/run-node-tests.mjs` as an ESM Node script using only built-in modules. It should accept one or more directory arguments, discover files ending in `.test.mjs`, run them through `node --test`, stream output, and exit with the child status. If no tests exist yet, print a clear message and exit 0 so the foundation command is runnable before Phase 2 adds real tests. Create `tests/server/README.md` documenting that future server/data tests should use Node's built-in `node:test` initially, place focused tests under `tests/server/`, and avoid provider secrets or real network calls. Add `tests/server/.gitkeep` so the directory exists.</action>
  <verify>
    <automated>node scripts/run-node-tests.mjs tests/server</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/run-node-tests.mjs` uses only Node built-in modules.
    - `node scripts/run-node-tests.mjs tests/server` exits 0 when no tests are present.
    - `tests/server/README.md` documents scope, naming, and no-network/no-secret rules.
  </acceptance_criteria>
  <done>Focused server/data test path exists and is runnable without installing new tooling.</done>
</task>

<task type="auto">
  <name>Task 3: Record current quality blockers</name>
  <files>.planning/phases/01-explore-scope-and-verification-foundation/01-VERIFICATION-BASELINE.md</files>
  <read_first>.planning/phases/01-explore-scope-and-verification-foundation/01-RESEARCH.md, .planning/codebase/CONCERNS.md, .planning/codebase/TESTING.md</read_first>
  <action>Create `01-VERIFICATION-BASELINE.md` summarizing the current `eslint` and `nuxi typecheck` results from `01-RESEARCH.md`. Include exact commands, high-level failure categories, and a section named `Explore-Relevant Blockers` that lists the Explore filename/style blockers and notes whether they are listed or resolved. Include a section named `Unrelated Existing Blockers` for typecheck failures outside Explore so future phases can distinguish scoped failures from repository-wide debt. This satisfies FOUND-03 without pretending the repo is globally green.</action>
  <verify>
    <automated>Test-Path .planning/phases/01-explore-scope-and-verification-foundation/01-VERIFICATION-BASELINE.md</automated>
    <automated>rg --fixed-strings "Explore-Relevant Blockers" .planning/phases/01-explore-scope-and-verification-foundation/01-VERIFICATION-BASELINE.md</automated>
    <automated>rg --fixed-strings "Unrelated Existing Blockers" .planning/phases/01-explore-scope-and-verification-foundation/01-VERIFICATION-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - Baseline document includes the lint and typecheck commands used.
    - Baseline document mentions `components/explore`, `pages/explore.vue`, `composables/useMapbox.ts`, and `composables/useRouteGenerator.ts`.
    - Baseline document separates Explore-relevant blockers from unrelated historical blockers.
  </acceptance_criteria>
  <done>Current blockers are captured so later phases can verify new work against an honest baseline.</done>
</task>

</tasks>

<verification>
- [ ] `node -e "const pkg=require('./package.json'); for (const key of ['lint:source','test:server','verify:explore-foundation']) { if (!pkg.scripts?.[key]) throw new Error('missing '+key) }"` passes.
- [ ] `node scripts/run-node-tests.mjs tests/server` passes.
- [ ] `rg --fixed-strings "Explore-Relevant Blockers" .planning/phases/01-explore-scope-and-verification-foundation/01-VERIFICATION-BASELINE.md` finds the baseline section.
</verification>

<success_criteria>

- FOUND-01 is covered by artifact exclusions and a scoped source lint script.
- FOUND-02 is covered by a focused server/data test command and directory pattern.
- FOUND-03 is covered by an explicit baseline of current lint/typecheck blockers.
- No new dependencies are added.

</success_criteria>

<output>
After completion, create `.planning/phases/01-explore-scope-and-verification-foundation/01-01-SUMMARY.md`
</output>
