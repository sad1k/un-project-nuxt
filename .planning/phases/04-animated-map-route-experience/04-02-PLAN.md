---
phase: 04-animated-map-route-experience
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - composables/use-mapbox.ts
  - components/explore/route-marker.ts
  - pages/explore.vue
autonomous: true
requirements:
  - MAP-01
  - MAP-02
  - MAP-04
  - MAP-05
  - MAP-06
user_setup: []
must_haves:
  truths:
    - D-01/D-03 generated route markers appear progressively from active route points without exposing raw SSE JSON.
    - D-02 a GeoJSON route line connects generated places in route order and uses useful zoom/focus for detailed line display.
    - D-05 a selected day updates the rendered route section instead of always showing the full route.
    - D-09 current-location or user-owned markers use a distinct visual marker kind when shown.
  artifacts:
    - composables/use-mapbox.ts
    - components/explore/route-marker.ts
    - pages/explore.vue
  key_links:
    - pages/explore.vue uses lib/explore/route-map.ts to feed use-mapbox.ts.
    - use-mapbox.ts renders route line and leg label GeoJSON sources from the shared route-map model.
---

<objective>
Upgrade the Explore map renderer from prototype full redraws to route-section-aware marker, line, label, and camera behavior.

Purpose: Make Phase 3 route events visible on the map as a progressive route experience, with zoom-aware line detail and distinct marker styling.
Output: Updated Mapbox composable, marker factory, and Explore page wiring.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-animated-map-route-experience/04-CONTEXT.md
@.planning/phases/04-animated-map-route-experience/04-RESEARCH.md
@.planning/phases/04-animated-map-route-experience/04-01-SUMMARY.md
@lib/explore/route-map.ts
@lib/ai/route-contract.ts
@pages/explore.vue
@composables/use-mapbox.ts
@components/explore/route-marker.ts
@composables/use-ai-route-session.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retarget markers to the route-map model</name>
  <files>components/explore/route-marker.ts, composables/use-mapbox.ts</files>
  <read_first>lib/explore/route-map.ts, components/explore/route-marker.ts, composables/use-mapbox.ts</read_first>
  <action>Replace the old `~/composables/use-route-generator` marker type dependency with the new route-map model from `lib/explore/route-map.ts`. Update marker creation so generated route points remain numbered, while `current-location` and `user-place` marker kinds have visibly distinct color/shape/icon treatment. Keep marker HTML compact and avoid rich place popup scope because Phase 5 owns rich popups.</action>
  <verify>pnpm lint:source -- components/explore/route-marker.ts composables/use-mapbox.ts if supported by the script, otherwise pnpm lint:source</verify>
  <acceptance_criteria>
    - `components/explore/route-marker.ts` no longer imports `~/composables/use-route-generator`.
    - Marker creation accepts a route-map point or marker kind from `lib/explore/route-map.ts`.
    - Generated, current-location, and user-place markers are visually distinguishable in code.
  </acceptance_criteria>
  <done>Marker rendering uses Phase 4 route-map types and supports distinct marker kinds.</done>
</task>

<task type="auto">
  <name>Task 2: Add route line and leg label layer rendering</name>
  <files>composables/use-mapbox.ts</files>
  <read_first>composables/use-mapbox.ts, lib/explore/route-map.ts, .planning/phases/04-animated-map-route-experience/04-RESEARCH.md</read_first>
  <action>Replace or extend `drawRouteLine(points)` with an idempotent route renderer that updates GeoJSON sources for route line and leg labels. Use one line source/layer for ordered route coordinates and one point source plus `symbol` layer for leg distance labels. Preserve cleanup by removing route, route-label, and marker state in `destroy()` and relevant clear functions. Do not render labels for missing distances. Keep layer ids stable, for example `explore-route-line` and `explore-route-leg-labels`, to avoid duplicate layers during SSE updates. Include D-02 zoom/focus gating so detailed line and label rendering is useful at close map zooms without cluttering overview mode.</action>
  <verify>rg -n "explore-route-line|explore-route-leg-labels|setData|symbol|text-field" composables/use-mapbox.ts</verify>
  <acceptance_criteria>
    - Route line data is represented as GeoJSON and updated without adding duplicate source/layer ids.
    - Leg labels use a GeoJSON point source and a symbol layer with text from distance labels.
    - Missing distance data does not create a fabricated on-map label.
    - `destroy()` and route clearing paths remove route line and leg label layers/sources.
  </acceptance_criteria>
  <done>Mapbox renderer can draw selected route sections with distance labels.</done>
</task>

<task type="auto">
  <name>Task 3: Wire progressive route rendering in Explore page</name>
  <files>pages/explore.vue</files>
  <read_first>pages/explore.vue, composables/use-ai-route-session.ts, lib/explore/route-map.ts, composables/use-mapbox.ts</read_first>
  <action>Update `pages/explore.vue` to build route-map points, route legs, and selected route section from `useAiRouteSession().activePoints` using `lib/explore/route-map.ts`. Keep route rendering point-by-point as active points arrive. Avoid calling camera fit on every single SSE point after the first meaningful route fit; use a small guard or route-completed/point-count strategy so progressive rendering does not constantly reset user zoom. Pass selected-day state through a composable or page-level ref so Plan 03 can connect the segmented selector.</action>
  <verify>rg -n "filterRoutePointsByDay|buildRouteLegs|renderRoute|selectedDay|fitToRoute" pages/explore.vue</verify>
  <acceptance_criteria>
    - `pages/explore.vue` no longer maps Phase 3 points into the old mocked `RoutePoint` shape.
    - Route rendering updates when active SSE points arrive.
    - Selected-day state can influence which route section is rendered.
    - Camera fitting does not blindly fire on every route point update forever.
  </acceptance_criteria>
  <done>Explore page feeds progressive route-map data into the Mapbox renderer.</done>
</task>

</tasks>

<verification>
- [ ] pnpm test:server
- [ ] pnpm lint:source
- [ ] pnpm typecheck
- [ ] Manual code inspection confirms Mapbox sources/layers are updated idempotently and cleaned up on destroy.
</verification>

<success_criteria>
- Generated route markers and route lines render from Phase 3 active route points.
- Route line and leg-label layers are GeoJSON-backed and update for selected route sections.
- Detailed route labels are zoom/focus-aware enough to keep overview mode calm.
- Current/user-owned point styling is available without mixing saved places into the route map by default.
</success_criteria>

<output>
After completion, create `.planning/phases/04-animated-map-route-experience/04-02-SUMMARY.md`
</output>
