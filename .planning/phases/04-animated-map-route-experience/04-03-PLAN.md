---
phase: 04-animated-map-route-experience
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - components/explore/route-panel.vue
  - components/explore/route-day-selector.vue
  - components/explore/route-distance-summary.vue
  - pages/explore.vue
autonomous: false
requirements:
  - MAP-03
  - MAP-04
  - MAP-05
  - MAP-06
user_setup: []
must_haves:
  truths:
    - D-04 day-by-day grouping is visible as a segmented selector.
    - D-05/D-06 selecting a day isolates that route section on both the map and sidebar.
    - D-10/D-11/D-12 sidebar shows per-leg distance details and a route-level summary when data exists.
    - D-07/D-08/D-09 saved places remain in explicit context-picking UI and are not confused with generated route markers during route viewing.
  artifacts:
    - components/explore/route-day-selector.vue
    - components/explore/route-distance-summary.vue
    - components/explore/route-panel.vue
  key_links:
    - route-panel.vue shares selected-day state with pages/explore.vue route rendering.
    - route-distance-summary.vue consumes leg and summary helpers from lib/explore/route-map.ts.
---

<objective>
Build the route controls and visible distance UX around the animated map experience, then verify the whole Phase 4 flow in browser.

Purpose: Give the user a compact day selector, selected-section isolation, and distance context in the sidebar while preserving a clean generated-route map.
Output: Route day selector, distance summary UI, route-panel integration, and a human visual verification checkpoint.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-animated-map-route-experience/04-CONTEXT.md
@.planning/phases/04-animated-map-route-experience/04-RESEARCH.md
@.planning/phases/04-animated-map-route-experience/04-01-SUMMARY.md
@.planning/phases/04-animated-map-route-experience/04-02-SUMMARY.md
@lib/explore/route-map.ts
@components/explore/route-panel.vue
@pages/explore.vue
@composables/use-ai-route-session.ts
@components/explore/context-selector.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add segmented day selector</name>
  <files>components/explore/route-day-selector.vue, components/explore/route-panel.vue, pages/explore.vue</files>
  <read_first>lib/explore/route-map.ts, components/explore/route-panel.vue, pages/explore.vue</read_first>
  <action>Create `components/explore/route-day-selector.vue` as a compact segmented control for all route days plus an optional "All" state only if it does not undermine the selected-day isolation decision. Wire it into `route-panel.vue` using `v-model` or explicit props/events. Keep selected-day state shared with `pages/explore.vue` so selecting a day filters both the sidebar route rows and the map route section.</action>
  <verify>rg -n "route-day-selector|selectedDay|Day" components/explore/route-panel.vue pages/explore.vue components/explore/route-day-selector.vue</verify>
  <acceptance_criteria>
    - Day selector is visible only when route points contain at least one day group.
    - Selecting day N filters sidebar route rows to day N.
    - The same selected day affects map rendering through `pages/explore.vue`.
    - Control uses compact segmented-button styling, not a large card or marketing-style block.
  </acceptance_criteria>
  <done>Day grouping is selectable and controls map/sidebar route section isolation.</done>
</task>

<task type="auto">
  <name>Task 2: Add sidebar distance summary and leg rows</name>
  <files>components/explore/route-distance-summary.vue, components/explore/route-panel.vue</files>
  <read_first>lib/explore/route-map.ts, components/explore/route-panel.vue, .planning/phases/04-animated-map-route-experience/04-CONTEXT.md</read_first>
  <action>Create `components/explore/route-distance-summary.vue` to show route total distance and per-leg labels from `buildRouteLegs` and `summarizeRouteDistance`. Integrate it into `route-panel.vue` near route stats/list. Show unavailable/partial distance states without inventing values. Keep labels short enough for the 400px sidebar and mobile-friendly if the panel layout changes later.</action>
  <verify>rg -n "route-distance-summary|total|distance|leg" components/explore/route-distance-summary.vue components/explore/route-panel.vue</verify>
  <acceptance_criteria>
    - Sidebar shows per-leg distances when leg distance labels exist.
    - Sidebar shows a route-level summary distance when at least one leg distance exists.
    - Sidebar gracefully handles missing distance estimates.
    - No cost/review/photo/weather popup scope is added in this phase.
  </acceptance_criteria>
  <done>Sidebar exposes per-leg and summary distance information.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve generated-route focus and context-selection boundary</name>
  <files>components/explore/route-panel.vue, components/explore/context-selector.vue, pages/explore.vue</files>
  <read_first>.planning/phases/04-animated-map-route-experience/04-CONTEXT.md, components/explore/context-selector.vue, pages/explore.vue</read_first>
  <action>Verify and adjust the UI so route viewing focuses on generated route points. Keep saved places and diary logs in `context-selector.vue` for explicit route setting/context selection. If current location is shown on the map, use the distinct marker kind from Plan 02; otherwise leave current location as selected context only. Do not add a saved-places overlay that mixes all saved places into the generated route map by default.</action>
  <verify>rg -n "context-selector|current-location|user-place|generated" components/explore pages/explore.vue</verify>
  <acceptance_criteria>
    - Saved places remain selectable as route context.
    - Generated route markers remain the primary map markers during route viewing.
    - Any current-location map marker is visually distinct from generated route markers.
    - No code path silently overlays all saved places onto the generated route map by default.
  </acceptance_criteria>
  <done>MAP-06 is implemented without confusing generated and user-owned places.</done>
</task>

<task type="auto">
  <name>Task 4: Run focused automated verification</name>
  <files>tests/server/explore-route-map.test.mjs, components/explore/route-panel.vue, pages/explore.vue, composables/use-mapbox.ts</files>
  <action>Run the smallest meaningful automated checks after implementation. Start with `pnpm test:server`, then run `pnpm lint:source` and `pnpm typecheck`. If full lint/typecheck fail due to existing unrelated issues, record the exact blockers in the plan summary and still report whether Phase 4 files pass source-level inspection.</action>
  <verify>pnpm test:server; pnpm lint:source; pnpm typecheck</verify>
  <acceptance_criteria>
    - Server/source tests pass or failures are clearly identified as unrelated existing blockers.
    - Phase 4 route-map helper tests pass.
    - No secret-bearing `.env` content is read or quoted.
  </acceptance_criteria>
  <done>Automated verification evidence is collected.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Animated Explore map route experience with progressive route markers, route line, day segmented selector, selected-day isolation, and distance labels/summary.</what-built>
  <how-to-verify>Start the Nuxt dev server, open `/explore`, generate or mock a route, and verify: route points appear progressively; route line detail and leg labels are readable at useful zoom; selecting a day isolates that route section on map and sidebar; sidebar shows per-leg and summary distance; saved places are context-selection UI, not confusing default route markers; current location, if shown, is styled differently from generated route points.</how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] pnpm test:server
- [ ] pnpm lint:source
- [ ] pnpm typecheck
- [ ] Browser check of `/explore` verifies progressive route reveal, selected-day isolation, route line labels, and sidebar distance details.
</verification>

<success_criteria>
- MAP-03 day grouping is visible and selectable through a segmented control.
- MAP-04 selected route/day changes animate or focus the map without resetting user zoom constantly.
- MAP-05 distance appears both on map leg labels and in sidebar per-leg/summary UI.
- MAP-06 saved/context/current-location places are visually and behaviorally distinct from generated route points.
- Human visual verification is completed before Phase 4 is declared done.
</success_criteria>

<output>
After completion, create `.planning/phases/04-animated-map-route-experience/04-03-SUMMARY.md`
</output>
