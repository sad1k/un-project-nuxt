---
phase: 04-animated-map-route-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/explore/route-map.ts
  - tests/server/explore-route-map.test.mjs
autonomous: true
requirements:
  - MAP-01
  - MAP-02
  - MAP-03
  - MAP-05
  - MAP-06
user_setup: []
must_haves:
  truths:
    - D-04/D-05/D-06 route points can be grouped by day and filtered to one selected day without losing route order.
    - D-10/D-11/D-12 route legs expose per-leg distance labels and route-level summary distance only when distance metadata exists.
    - D-07/D-08/D-09 generated route points, current location, and user-owned points have distinct marker kinds in the map model.
  artifacts:
    - lib/explore/route-map.ts
    - tests/server/explore-route-map.test.mjs
  key_links:
    - lib/explore/route-map.ts consumes RoutePoint from lib/ai/route-contract.ts.
    - tests/server/explore-route-map.test.mjs protects the route display transforms before Mapbox/UI wiring.
---

<objective>
Create the typed route-map display model that Phase 4 UI and Mapbox rendering will share.

Purpose: Keep day filtering, leg labels, distance summaries, and marker-kind distinctions testable outside Mapbox/Vue rendering.
Output: A pure `lib/explore/route-map.ts` helper module plus focused regression tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-animated-map-route-experience/04-CONTEXT.md
@.planning/phases/04-animated-map-route-experience/04-RESEARCH.md
@.planning/phases/03-ai-route-generation-and-streaming/03-CONTEXT.md
@lib/ai/route-contract.ts
@composables/use-ai-route-session.ts
@tests/server/ai-route-contract.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add route map helper tests first</name>
  <files>tests/server/explore-route-map.test.mjs</files>
  <read_first>tests/server/ai-route-contract.test.mjs, lib/ai/route-contract.ts, .planning/phases/04-animated-map-route-experience/04-CONTEXT.md</read_first>
  <action>Create focused `node:test` coverage for a new `lib/explore/route-map.ts` helper. Cover these cases with concrete fixture points: D-04 day groups are derived as `[1, 2, 3]`; D-05 selecting day 2 returns only day 2 points; D-10 route legs preserve point order and expose labels from `approximateDistanceMeters`; D-11 total distance sums known legs and ignores missing estimates; D-12 missing distance returns an explicit unavailable state; D-07/D-09 marker models can represent `generated`, `current-location`, and `user-place` kinds.</action>
  <verify>pnpm test:server tests/server/explore-route-map.test.mjs fails before helper implementation and passes after Task 2.</verify>
  <acceptance_criteria>
    - `tests/server/explore-route-map.test.mjs` imports from `lib/explore/route-map.ts`.
    - Tests mention selected-day filtering, route legs, distance summary, missing distance, and marker kind distinction.
    - Tests do not require Mapbox, Vue, browser globals, or provider credentials.
  </acceptance_criteria>
  <done>Route-map behavior is specified by focused tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement route display model helpers</name>
  <files>lib/explore/route-map.ts</files>
  <read_first>lib/ai/route-contract.ts, tests/server/explore-route-map.test.mjs</read_first>
  <action>Implement a pure TypeScript helper module. Export concrete types and functions for route display: `RouteMarkerKind`, `RouteMapPoint`, `RouteLeg`, `RouteDayGroup`, `RouteDistanceSummary`, `toRouteMapPoints(points)`, `getRouteDayGroups(points)`, `filterRoutePointsByDay(points, selectedDay)`, `buildRouteLegs(points)`, `summarizeRouteDistance(legs)`, and `formatRouteDistance(meters)`. Use `RoutePoint.coordinates.long` as longitude. Never fabricate distance; if `approximateDistanceMeters` is missing, expose an unavailable/null label state.</action>
  <verify>pnpm test:server tests/server/explore-route-map.test.mjs</verify>
  <acceptance_criteria>
    - `lib/explore/route-map.ts` imports the `RoutePoint` type from `~/lib/ai/route-contract` or an equivalent relative path.
    - The helper module contains no Vue refs, computed state, Mapbox imports, browser globals, fetch calls, env reads, or console logging.
    - Day filtering and distance formatting are deterministic and covered by tests.
  </acceptance_criteria>
  <done>Pure route-map model exists and tests pass.</done>
</task>

</tasks>

<verification>
- [ ] pnpm test:server tests/server/explore-route-map.test.mjs
- [ ] pnpm test:server
- [ ] rg -n "ref\\(|computed\\(|mapbox|fetch\\(|process\\.env|console\\." lib/explore/route-map.ts returns no matches
</verification>

<success_criteria>
- `lib/explore/route-map.ts` provides the shared map/sidebar route model.
- Tests prove MAP-03 selected-day filtering, MAP-05 distance behavior, and MAP-06 marker-kind distinction at the data-model level.
- No UI, Mapbox, or provider dependency leaks into the helper module.
</success_criteria>

<output>
After completion, create `.planning/phases/04-animated-map-route-experience/04-01-SUMMARY.md`
</output>
