---
phase: 06-save-to-diary-and-release-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db/schema/route-diary-save.ts
  - lib/db/schema/index.ts
  - lib/db/queries/route-diary-save.ts
  - lib/db/queries/location.ts
  - lib/db/queries/location-log.ts
  - lib/ai/route-generation-runner.ts
  - tests/server/route-diary-save.test.mjs
autonomous: true
requirements:
  - DIARY-01
  - DIARY-02
  - DIARY-03
  - OBS-03
user_setup:
  - "If remote Turso schema push is still blocked, verify schema changes against file:local.db and record the remote credential blocker in the summary."
must_haves:
  truths:
    - D-01 saved generated routes use the existing diary structures instead of a separate diary product model.
    - D-02 each generated route point becomes one authenticated user's locationLog.
    - D-03 route-point diary import creates or reuses a matching authenticated user's location before inserting the locationLog.
    - D-04 the whole generated route is saved, not only selected independent places.
    - D-05 completed generated routes automatically save to the user's diary.
    - D-07 automatic diary save is idempotent across retries, reloads, and route-history restore.
    - D-09 saved route points remain associated with the authenticated user through both location and locationLog records.
  artifacts:
    - lib/db/queries/route-diary-save.ts
    - lib/db/schema/route-diary-save.ts
    - lib/ai/route-generation-runner.ts
    - tests/server/route-diary-save.test.mjs
  key_links:
    - route-generation-runner.ts triggers automatic diary save only after terminal route completion.
    - route-diary-save query/service reads user-owned route variants and writes user-owned diary records.
    - route-diary-save metadata prevents duplicate locationLog rows for the same route point.
---

<objective>
Build idempotent automatic route-to-diary persistence.

Purpose: Completed AI-generated routes should automatically create diary records for the authenticated user, with one diary log per generated route point.
Output: Route diary-save schema/query/service, route completion integration, local schema update, and focused ownership/idempotency tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-save-to-diary-and-release-hardening/06-CONTEXT.md
@.planning/phases/06-save-to-diary-and-release-hardening/06-RESEARCH.md
@.planning/phases/05.1-route-generation-continuity-and-completion-notifications/05.1-CONTEXT.md
@.planning/phases/05.1-route-generation-continuity-and-completion-notifications/05.1-VERIFICATION.md
@lib/db/schema/ai-route.ts
@lib/db/queries/ai-route.ts
@lib/db/schema/location.ts
@lib/db/schema/location-log.ts
@lib/db/queries/location.ts
@lib/db/queries/location-log.ts
@lib/ai/route-generation-runner.ts
@tests/server/ai-route-continuity.test.mjs
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-0601-01 | Automatic save creates duplicate diary logs when route completion is retried or restored. | high | Add idempotency metadata keyed by user, session, variant, and route point id before/with diary inserts. |
| T-0601-02 | A route owned by one user creates diary records for another user. | high | Re-read route session/variant/points using authenticated `userId`; write `location` and `locationLog` with the same `userId`. |
| T-0601-03 | Route point names collide with existing locations and overwrite or reuse the wrong place. | medium | Use user-scoped lookup/reuse and deterministic slug collision handling; never query by name without `userId`. |
| T-0601-04 | Auto-save failure hides a completed route from diary without evidence. | medium | Persist safe save status/error code and surface it to later plans without logging raw route/private context. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Lock diary import expectations with focused tests</name>
  <files>tests/server/route-diary-save.test.mjs</files>
  <read_first>.planning/phases/06-save-to-diary-and-release-hardening/06-CONTEXT.md, tests/server/ai-route-persistence.test.mjs, tests/server/ai-route-continuity.test.mjs</read_first>
  <action>Create focused `node:test` source-level coverage for Phase 6 diary import. Assert a route diary-save query/service exists; it references `aiRouteSession`, `aiRouteVariant`, `aiRoutePoint`, `location`, and `locationLog`; it filters by `userId`; it records idempotency by route/session/variant/point identity; and `route-generation-runner.ts` triggers diary save after `markAiRouteVariantCompleted`. The test should fail before implementation and pass after Tasks 2-4.</action>
  <verify>node scripts/run-node-tests.mjs tests/server/route-diary-save.test.mjs</verify>
  <acceptance_criteria>
    - Test names or assertions reference DIARY-01, DIARY-02, DIARY-03, and OBS-03.
    - Tests check automatic whole-route save and one-log-per-route-point behavior.
    - Tests check idempotency and user-owned query filters.
    - Tests do not call provider APIs, remote Turso, browser APIs, or network.
  </acceptance_criteria>
  <done>Diary save behavior is locked before implementation edits.</done>
</task>

<task type="auto">
  <name>Task 2: Add idempotency metadata for route diary saves</name>
  <files>lib/db/schema/route-diary-save.ts, lib/db/schema/index.ts</files>
  <read_first>lib/db/schema/ai-route.ts, lib/db/schema/location.ts, lib/db/schema/location-log.ts, tests/server/route-diary-save.test.mjs</read_first>
  <action>Add the smallest schema needed to record automatic route diary imports. Prefer a table such as `routeDiarySave` with `userId`, `sessionId`, `variantId`, `routePointId`, `locationId`, `locationLogId`, `status`, `failureCode`, `createdAt`, and `updateAt`. Add a user-scoped unique key that prevents duplicate save rows for the same generated route point. Export the schema from `lib/db/schema/index.ts`.</action>
  <verify>node scripts/run-node-tests.mjs tests/server/route-diary-save.test.mjs && rg -n "routeDiarySave|routePointId|locationLogId|variantId|userId" lib/db/schema/route-diary-save.ts lib/db/schema/index.ts</verify>
  <acceptance_criteria>
    - Idempotency can be enforced per user/session/variant/routePointId.
    - Save rows can point to created `location` and `locationLog` records.
    - Failure status can be recorded without raw route context.
    - New schema exports from the schema barrel.
  </acceptance_criteria>
  <done>Diary save metadata can prevent duplicate route-point logs.</done>
</task>

<task type="auto">
  <name>Task 3: Implement route-point to diary persistence</name>
  <files>lib/db/queries/route-diary-save.ts, lib/db/queries/location.ts, lib/db/queries/location-log.ts</files>
  <read_first>lib/db/queries/ai-route.ts, lib/db/queries/location.ts, lib/db/queries/location-log.ts, lib/db/schema/location.ts, lib/db/schema/location-log.ts</read_first>
  <action>Create a route diary-save query/service that accepts `userId`, `sessionId`, and `variantId`, re-reads the route/variant/points through ownership-safe queries, creates or reuses user-owned `location` records for each point, inserts one `locationLog` per point, and records idempotency metadata. Preserve route/session provenance in the log description or metadata where possible without adding sensitive raw context. If existing location helpers need a create-or-reuse operation, add it with user-scoped lookup and deterministic slug handling.</action>
  <verify>node scripts/run-node-tests.mjs tests/server/route-diary-save.test.mjs</verify>
  <acceptance_criteria>
    - Completed route points become one `locationLog` each.
    - `location` creation/reuse is scoped to authenticated `userId`.
    - Re-running the service for the same session/variant does not create duplicate logs.
    - The service returns compact save summary suitable for UI use.
  </acceptance_criteria>
  <done>Generated route variants can be imported into diary records safely and idempotently.</done>
</task>

<task type="auto">
  <name>Task 4: Trigger automatic save after route completion</name>
  <files>lib/ai/route-generation-runner.ts, lib/db/queries/route-diary-save.ts</files>
  <read_first>lib/ai/route-generation-runner.ts, lib/db/queries/route-diary-save.ts, .planning/phases/06-save-to-diary-and-release-hardening/06-CONTEXT.md</read_first>
  <action>Call the route diary-save service after a variant is marked completed in `runRouteGeneration`. The completion path must keep route generation terminal status even if diary import fails; record a safe diary-save failure code/status that Plan 02 can surface. Do not log raw route points, prompt content, provider text, requestContextJson, or payloadJson.</action>
  <verify>node scripts/run-node-tests.mjs tests/server/route-diary-save.test.mjs tests/server/ai-route-continuity.test.mjs</verify>
  <acceptance_criteria>
    - Completed generated routes automatically start diary persistence.
    - Diary save failure does not roll back route completion.
    - Failure details are safe error codes/counts only.
    - Existing route continuity tests still pass.
  </acceptance_criteria>
  <done>Route completion automatically saves whole generated routes into diary.</done>
</task>

<task type="auto">
  <name>Task 5: Verify local schema and focused suite</name>
  <files>local.db, tests/server/route-diary-save.test.mjs</files>
  <read_first>drizzle.config.ts, package.json, .planning/phases/05.1-route-generation-continuity-and-completion-notifications/05.1-VERIFICATION.md</read_first>
  <action>Run local schema push after schema edits, then run focused diary and route persistence/continuity tests. If remote Turso push is attempted and fails with the known 401 credential blocker, document it in the summary rather than broadening the phase.</action>
  <verify>TURSO_DATABASE_URL=file:local.db NODE_ENV=development pnpm exec drizzle-kit push && node scripts/run-node-tests.mjs tests/server/route-diary-save.test.mjs tests/server/ai-route-persistence.test.mjs tests/server/ai-route-continuity.test.mjs</verify>
  <acceptance_criteria>
    - Local database accepts the route diary-save schema.
    - Focused diary save tests pass.
    - Existing route persistence and continuity tests still pass.
    - Any remote schema blocker is recorded honestly.
  </acceptance_criteria>
  <done>Diary save core is locally verifiable and ready for UI surfacing.</done>
</task>

</tasks>

<verification>
- [ ] node scripts/run-node-tests.mjs tests/server/route-diary-save.test.mjs
- [ ] node scripts/run-node-tests.mjs tests/server/ai-route-persistence.test.mjs tests/server/ai-route-continuity.test.mjs
- [ ] TURSO_DATABASE_URL=file:local.db NODE_ENV=development pnpm exec drizzle-kit push
- [ ] pnpm test:server
- [ ] pnpm lint:source
</verification>

<success_criteria>
- DIARY-01 is covered by automatic diary persistence after route completion.
- DIARY-02 is covered by route points saved into existing `location`/`locationLog` structures.
- DIARY-03 and OBS-03 are covered by user-scoped route reads and diary writes plus focused ownership/idempotency tests.
</success_criteria>

<output>
After completion, create `.planning/phases/06-save-to-diary-and-release-hardening/06-01-SUMMARY.md`
</output>
