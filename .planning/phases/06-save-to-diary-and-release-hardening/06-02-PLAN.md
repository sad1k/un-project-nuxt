---
phase: 06-save-to-diary-and-release-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - lib/db/queries/route-diary-save.ts
  - lib/db/queries/ai-route.ts
  - server/api/ai/route-sessions.get.ts
  - server/api/ai/route/[session-id].get.ts
  - composables/use-ai-route-session.ts
  - composables/use-route-generation-status.ts
  - components/explore/route-history.vue
  - components/explore/route-panel.vue
  - tests/server/route-diary-ui.test.mjs
autonomous: true
requirements:
  - DIARY-01
  - DIARY-02
  - DIARY-03
user_setup: []
must_haves:
  truths:
    - D-06 route history remains automatic and Phase 6 connects completed route history to diary persistence.
    - D-08 the default save unit is the whole generated route.
    - D-10 the route is automatically saved to route history and diary after successful generation.
    - D-11 the saved state is visible from route history/completed-session surfaces and the Explore route sidebar.
    - D-12 exact copy and navigation links are flexible, but users must be able to discover the saved diary outcome.
  artifacts:
    - server/api/ai/route-sessions.get.ts
    - server/api/ai/route/[session-id].get.ts
    - components/explore/route-history.vue
    - components/explore/route-panel.vue
    - tests/server/route-diary-ui.test.mjs
  key_links:
    - Route session summaries expose diary save status.
    - Route history and route panel render saved-to-diary status without requiring an explicit save button.
    - Session restore includes enough diary save data to link or summarize created diary records.
---

<objective>
Surface automatic saved-to-diary status in Explore.

Purpose: Users should understand that completed generated routes have already been saved into their diary and should be able to find the saved diary outcome from route history or the route panel.
Output: Diary-save summary data on route session APIs, client state wiring, route history/sidebar UI, and focused source tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/06-save-to-diary-and-release-hardening/06-CONTEXT.md
@.planning/phases/06-save-to-diary-and-release-hardening/06-RESEARCH.md
@.planning/phases/06-save-to-diary-and-release-hardening/06-01-SUMMARY.md
@lib/db/queries/route-diary-save.ts
@lib/db/queries/ai-route.ts
@server/api/ai/route-sessions.get.ts
@server/api/ai/route/[session-id].get.ts
@composables/use-ai-route-session.ts
@composables/use-route-generation-status.ts
@components/explore/route-history.vue
@components/explore/route-panel.vue
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-0602-01 | User cannot tell whether automatic diary save happened. | medium | Add visible saved/failed/pending diary save status to route history and route panel. |
| T-0602-02 | UI exposes another user's diary record ids or route state. | high | Only use authenticated route-session APIs and user-owned save summaries from Plan 01. |
| T-0602-03 | UI offers a misleading manual save action even though automatic save is locked. | medium | Present status and navigation/retry affordances, not a primary "Save route" button for completed routes. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Lock saved-state UI expectations</name>
  <files>tests/server/route-diary-ui.test.mjs</files>
  <read_first>tests/server/ai-route-status.test.mjs, tests/server/route-generation-notifications.test.mjs, .planning/phases/06-save-to-diary-and-release-hardening/06-CONTEXT.md</read_first>
  <action>Create source-level tests that assert route session summaries/restores expose diary-save status from the route diary-save layer, `route-history.vue` renders saved-to-diary state for completed generations, and `route-panel.vue` surfaces diary status near the active route. Assert no primary explicit save button is required for completed generated routes.</action>
  <verify>node scripts/run-node-tests.mjs tests/server/route-diary-ui.test.mjs</verify>
  <acceptance_criteria>
    - Tests reference DIARY-01, DIARY-02, and DIARY-03.
    - Tests check route history and route panel saved-state rendering.
    - Tests check authenticated route-session API wiring rather than client-only fake state.
  </acceptance_criteria>
  <done>UI expectations are locked before implementation edits.</done>
</task>

<task type="auto">
  <name>Task 2: Expose diary save summaries through route APIs</name>
  <files>lib/db/queries/route-diary-save.ts, lib/db/queries/ai-route.ts, server/api/ai/route-sessions.get.ts, server/api/ai/route/[session-id].get.ts</files>
  <read_first>lib/db/queries/route-diary-save.ts, lib/db/queries/ai-route.ts, server/api/ai/route-sessions.get.ts, server/api/ai/route/[session-id].get.ts</read_first>
  <action>Add compact diary-save summary helpers and include their output in route session list and restore responses. Include status, saved point/log count, expected point count when available, safe failure code when present, and created diary links/ids only for the authenticated user. Keep response payload compact and avoid raw route context.</action>
  <verify>node scripts/run-node-tests.mjs tests/server/route-diary-ui.test.mjs tests/server/ai-route-status.test.mjs</verify>
  <acceptance_criteria>
    - Route sessions endpoint includes diary save status for each relevant session/variant.
    - Route restore endpoint includes diary save status for the active/restored variant.
    - All route/diary summary reads remain scoped by `event.context.user.id`.
    - Response does not include raw requestContextJson or route event payloads.
  </acceptance_criteria>
  <done>Client code can show automatic diary-save outcome from authenticated APIs.</done>
</task>

<task type="auto">
  <name>Task 3: Wire client state to diary save status</name>
  <files>composables/use-ai-route-session.ts, composables/use-route-generation-status.ts</files>
  <read_first>composables/use-ai-route-session.ts, composables/use-route-generation-status.ts, server/api/ai/route-sessions.get.ts</read_first>
  <action>Extend the route session client types/state to carry diary-save summary from status polling and restore responses. Preserve existing generation, notification, and route history behavior. Treat pending/failed diary saves as visible state rather than silently dropping them.</action>
  <verify>node scripts/run-node-tests.mjs tests/server/route-diary-ui.test.mjs tests/server/ai-route-client.test.mjs tests/server/ai-route-status.test.mjs</verify>
  <acceptance_criteria>
    - Active restored sessions expose diary save status to components.
    - Global route status polling carries saved-to-diary status for saved generations.
    - No client code references server-only provider credentials.
    - Existing route restore/status tests continue to pass.
  </acceptance_criteria>
  <done>Explore client state knows whether automatic diary save succeeded, is pending, or failed.</done>
</task>

<task type="auto">
  <name>Task 4: Render saved-to-diary state in route surfaces</name>
  <files>components/explore/route-history.vue, components/explore/route-panel.vue</files>
  <read_first>components/explore/route-history.vue, components/explore/route-panel.vue, components/explore/route-follow-up.vue</read_first>
  <action>Render compact saved-to-diary status in route history and the active route panel. Use a status badge/row with clear states for saved, pending, and failed. Where created diary log/location ids are available, provide links to existing dashboard diary pages. Do not add a primary explicit save button for completed generated routes because automatic save is locked.</action>
  <verify>node scripts/run-node-tests.mjs tests/server/route-diary-ui.test.mjs && rg -n "diary|saved|locationLog|routeDiary|Saved to diary" components/explore/route-history.vue components/explore/route-panel.vue</verify>
  <acceptance_criteria>
    - Completed route history entries show saved-to-diary status.
    - Active route panel shows saved/pending/failed diary status.
    - Failed automatic save is visible and actionable enough for later retry/debug.
    - UI remains compact and consistent with existing route history/sidebar controls.
  </acceptance_criteria>
  <done>Users can discover the automatic diary save outcome from Explore.</done>
</task>

<task type="auto">
  <name>Task 5: Run focused UI-state verification</name>
  <files>tests/server/route-diary-ui.test.mjs</files>
  <read_first>package.json, tests/server/route-diary-save.test.mjs</read_first>
  <action>Run focused diary-save and UI-state tests, then the broader server suite and source lint. Record any unrelated blockers in the plan summary.</action>
  <verify>node scripts/run-node-tests.mjs tests/server/route-diary-save.test.mjs tests/server/route-diary-ui.test.mjs && pnpm test:server && pnpm lint:source</verify>
  <acceptance_criteria>
    - Diary save core and UI state tests pass together.
    - Existing route status/client tests still pass.
    - Source lint passes or unrelated warnings are recorded.
  </acceptance_criteria>
  <done>Automatic diary-save state is visible and covered by focused tests.</done>
</task>

</tasks>

<verification>
- [ ] node scripts/run-node-tests.mjs tests/server/route-diary-save.test.mjs tests/server/route-diary-ui.test.mjs
- [ ] node scripts/run-node-tests.mjs tests/server/ai-route-client.test.mjs tests/server/ai-route-status.test.mjs
- [ ] pnpm test:server
- [ ] pnpm lint:source
</verification>

<success_criteria>
- DIARY-01 is visible to users through saved-to-diary status after route completion.
- DIARY-02 is visible through route point/log save summaries or links.
- DIARY-03 remains protected by authenticated route-session APIs and user-owned diary summary reads.
</success_criteria>

<output>
After completion, create `.planning/phases/06-save-to-diary-and-release-hardening/06-02-SUMMARY.md`
</output>
