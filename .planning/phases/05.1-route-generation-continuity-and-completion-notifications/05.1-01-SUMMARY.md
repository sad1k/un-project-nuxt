# Phase 5.1-01 Summary: Durable Route Generation Runner

## Completed

- Moved provider/mock route generation from `server/api/ai/route.post.ts` into `lib/ai/route-generation-runner.ts`.
- Kept the existing SSE response for live Explore users, but scheduled the runner through `event.waitUntil` so the work is not owned by the mounted Explore component.
- Added lifecycle fields on `aiRouteVariant`: started, heartbeat, completed, runner id, notification status, retry count.
- Added ownership-scoped query helpers for claiming a run, refreshing heartbeat, marking terminal state, and listing compact session summaries.

## Evidence

- `node scripts/run-node-tests.mjs tests/server/ai-route-continuity.test.mjs tests/server/ai-route-stream.test.mjs` passed.
- `pnpm exec drizzle-kit push --force` applied the DB schema changes. Drizzle still reports the existing `tsconfig.json` paths warning.

## Notes

- The stream writer deliberately does not backpressure the runner. If the browser leaves, write failures close the live stream path while the runner continues persistence.
