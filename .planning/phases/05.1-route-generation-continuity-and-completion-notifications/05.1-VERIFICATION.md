# Phase 5.1 Verification: Route Generation Continuity and Completion Notifications

## Requirement Evidence

| Requirement | Evidence | Status |
| --- | --- | --- |
| GENLIFE-01: Generation persists after leaving Explore | `server/api/ai/route.post.ts` schedules `runRouteGeneration` with `event.waitUntil`; runner owns provider/mock execution and DB persistence. | PASS |
| GENLIFE-02: Global progress indicator | `components/app/route-generation-indicator.vue` polls `server/api/ai/route-sessions.get.ts` through `useRouteGenerationStatus`. | PASS |
| GENLIFE-03: Return to completed/failed session | `/explore?sessionId=<id>` restores via `restoreRouteSession(explicitSessionId)`; route history lists recent sessions. | PASS |
| GENLIFE-04: In-app completion notification | `useRouteGenerationNotifications` watches terminal sessions, emits `wanderlog:route-generation-notification`, and renders notification state in the indicator. | PASS |
| GENLIFE-05: Browser push is permission/subscription gated | Push subscription schema/endpoints/service worker added; subscription only attempts when `ROUTE_NOTIFICATION_VAPID_PUBLIC_KEY`, service worker, PushManager, and permission exist. Server push delivery is deferred. | PARTIAL |
| GENLIFE-06: Ownership and sanitized observability | Status, restore, notification-status, and subscription endpoints use `defineAuthenticatedHandler`; tests assert ownership and no secret/provider-body logging in new runner paths. | PASS |

## Commands Run

- `node scripts/run-node-tests.mjs tests/server/ai-route-continuity.test.mjs tests/server/ai-route-status.test.mjs tests/server/route-generation-notifications.test.mjs` — PASS, 11 tests.
- `node scripts/run-node-tests.mjs tests/server/weather-tips.test.mjs` — PASS, 5 tests after normalizing item labels.
- `pnpm test:server` — PASS, 75 tests.
- `pnpm lint:source` — PASS with 11 existing console warnings in unrelated dashboard/location/map/S3 files.
- `pnpm typecheck` — FAIL due existing unrelated project type errors listed below.
- `pnpm exec drizzle-kit push --force` — PASS, schema changes applied; drizzle printed the existing `tsconfig.json` `paths` placement warning.
- `git diff --check` — PASS.
- Secret/token grep across new lifecycle/notification files — PASS, no secret env references found.

## Local Runtime Smoke

- Started Nuxt dev server on `http://localhost:3001`.
- `GET /explore` returned 200.
- `GET /route-generation-sw.js` returned 200.
- `GET /api/ai/route-sessions` returned 401 without auth, confirming the status list is authenticated.

## Typecheck Blockers

`pnpm typecheck` still fails outside the Phase 5.1 implementation surface:

- `components/animated-list.vue` slot/index typing errors.
- `components/feed/post-card.vue` and `components/image-list.vue` cannot resolve `vue-easy-lightbox` types.
- `components/file-upload.vue` and dashboard image page file upload handler type mismatch.
- `components/github-globe.vue` implicit `any[]`.
- `lib/ai/openai-compatible.ts` existing `HeadersInit` overload error.
- `lib/db/queries/location.ts`, `lib/db/schema/location.ts`, and `lib/db/schema/location-log.ts` existing zod/drizzle typing errors.
- Dashboard/location page prop and `never` inference errors.
- `server/api/sentry-example-api.ts` imports a missing `defineEventHandler` export.

## Manual Browser Checklist

- Start route generation on Explore, navigate away, and confirm global indicator stays visible: NOT FULLY EXERCISED in browser automation during this turn; local runtime smoke confirms the page and status endpoint are reachable.
- Return via indicator/history and confirm route restore: covered by source tests and `/explore?sessionId=<id>` wiring; not clicked manually.
- Confirm in-app notification appears once: covered by source tests; not clicked manually.
- Confirm browser push delivery: NOT IMPLEMENTED; only subscription/service-worker groundwork exists pending VAPID private key and sender implementation.
