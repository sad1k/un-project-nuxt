---
phase: 05.1-route-generation-continuity-and-completion-notifications
plan: 02
type: execute
wave: 2
depends_on:
  - 05.1-01
files_modified:
  - server/api/ai/route-sessions.get.ts
  - server/api/ai/route/[session-id].get.ts
  - lib/db/queries/ai-route.ts
  - composables/use-route-generation-status.ts
  - composables/use-ai-route-session.ts
  - components/app/route-generation-indicator.vue
  - components/app/nav-bar.vue
  - components/explore/route-history.vue
  - pages/explore.vue
  - tests/server/ai-route-status.test.mjs
autonomous: true
requirements:
  - GENLIFE-02
  - GENLIFE-03
  - GENLIFE-06
user_setup: []
must_haves:
  truths:
    - D-05 active generation status must be visible outside the Explore panel.
    - D-06 route history must include sessions, not only variants inside one restored session.
    - D-07 opening a completed background generation must restore the correct session and route points.
    - D-08 global status and history endpoints must never leak another user's route session.
  artifacts:
    - server/api/ai/route-sessions.get.ts
    - composables/use-route-generation-status.ts
    - components/app/route-generation-indicator.vue
    - components/explore/route-history.vue
    - tests/server/ai-route-status.test.mjs
  key_links:
    - route-sessions.get.ts provides compact user-owned summaries.
    - use-route-generation-status.ts is available to nav/default layout and Explore layout=false page.
    - route-history.vue distinguishes sessions from variants when needed.
---

<objective>
Expose active route-generation progress and recoverable route-session history in the interface.

Purpose: The user needs to see that generation is still running after leaving Explore and find the result after completion.
Output: User-owned status/history endpoint, global composable, nav/Explore indicator, session restore by query, and focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/05.1-route-generation-continuity-and-completion-notifications/05.1-CONTEXT.md
@.planning/phases/05.1-route-generation-continuity-and-completion-notifications/05.1-RESEARCH.md
@.planning/phases/05.1-route-generation-continuity-and-completion-notifications/05.1-01-SUMMARY.md
@server/api/ai/route/[session-id].get.ts
@lib/db/queries/ai-route.ts
@composables/use-ai-route-session.ts
@components/explore/route-history.vue
@components/explore/route-panel.vue
@components/app/nav-bar.vue
@layouts/default.vue
@pages/explore.vue
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-051-05 | Global status endpoint exposes another user's session ids or city history. | high | Use `defineAuthenticatedHandler` and `findAiRouteSessionsByUserId`-style filters only. |
| T-051-06 | Polling creates noisy load or UI flicker. | medium | Poll only while active jobs exist or when the app is visible; keep compact summaries. |
| T-051-07 | Session history is confused with route variant history. | medium | Label sessions by city/time/status and variants by route/refinement inside a session. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add status/history contract tests</name>
  <files>tests/server/ai-route-status.test.mjs</files>
  <read_first>tests/server/ai-route-restore.test.mjs, tests/server/ai-route-client.test.mjs, .planning/phases/05.1-route-generation-continuity-and-completion-notifications/05.1-CONTEXT.md</read_first>
  <action>Create source-level tests for the status/history slice. Assert there is an authenticated route-session listing endpoint, it reads through user-owned query helpers, it returns compact summary fields instead of raw event payloads, and client code exposes a global status composable/indicator. Assert Explore can restore a session from a route/query param or explicit session id, not only `sessionStorage`.</action>
  <verify>node scripts/run-node-tests.mjs tests/server/ai-route-status.test.mjs</verify>
  <acceptance_criteria>
    - Tests cover GENLIFE-02, GENLIFE-03, and GENLIFE-06.
    - Tests check for `defineAuthenticatedHandler`, user id filtering, compact summary fields, and absence of raw provider payloads.
    - Tests do not require a browser or network.
  </acceptance_criteria>
  <done>Status/history behavior is locked before UI/API edits.</done>
</task>

<task type="auto">
  <name>Task 2: Add compact user-owned route session listing endpoint</name>
  <files>server/api/ai/route-sessions.get.ts, lib/db/queries/ai-route.ts</files>
  <read_first>utils/define-authenticated-handler.ts, server/api/ai/route/[session-id].get.ts, lib/db/queries/ai-route.ts</read_first>
  <action>Create `server/api/ai/route-sessions.get.ts` using `defineAuthenticatedHandler`. Return compact summaries for the authenticated user: session id, city label/name, status, active variant id, active variant status, point count, created/updated timestamps, failure code, notification status, and whether it is currently active/stale. Add or reuse a query helper that avoids returning full event payload JSON. Support a small `activeOnly` query flag if useful for polling.</action>
  <verify>node scripts/run-node-tests.mjs tests/server/ai-route-status.test.mjs tests/server/ai-route-restore.test.mjs</verify>
  <acceptance_criteria>
    - Endpoint never returns sessions outside `event.context.user.id`.
    - Endpoint does not return raw `payloadJson`, raw request context, provider body previews, prompts, or headers.
    - Summaries are enough for global status/history UI.
    - Active/stale status is derived from lifecycle data from Plan 01.
  </acceptance_criteria>
  <done>The app can list active and previous route generations for the current user.</done>
</task>

<task type="auto">
  <name>Task 3: Add global route generation status composable</name>
  <files>composables/use-route-generation-status.ts, composables/use-ai-route-session.ts</files>
  <read_first>composables/use-ai-route-session.ts, composables/use-route-weather-tips.ts, composables/use-place-intelligence.ts</read_first>
  <action>Create `useRouteGenerationStatus()` with shared refs for summaries, active generation, last terminal transition, loading/error state, and refresh/poll controls. Poll while a generation is active and the document is visible; refresh on window focus/visibility change. Add a method to open/restore a selected session by id and update `useAiRouteSession()` so `restoreRouteSession(sessionId?: number)` can load explicit session ids as well as stored session ids.</action>
  <verify>node scripts/run-node-tests.mjs tests/server/ai-route-status.test.mjs tests/server/ai-route-client.test.mjs</verify>
  <acceptance_criteria>
    - Composable is client-safe and does not access browser APIs without `import.meta.client` guards.
    - Polling is bounded and disabled when there are no active jobs.
    - Explicit session restore updates active points/variants/events consistently.
    - Existing Explore restore behavior still works from `sessionStorage`.
  </acceptance_criteria>
  <done>Route generation status is available from any app surface.</done>
</task>

<task type="auto">
  <name>Task 4: Add global progress indicator and session history UI hooks</name>
  <files>components/app/route-generation-indicator.vue, components/app/nav-bar.vue, components/explore/route-history.vue, pages/explore.vue</files>
  <read_first>components/app/nav-bar.vue, layouts/default.vue, pages/explore.vue, components/explore/route-history.vue, components/explore/route-panel.vue</read_first>
  <action>Create `components/app/route-generation-indicator.vue` as a compact nav/global indicator with generating/completed/failed states and a link/action to open the relevant route session in Explore. Integrate it into `AppNavBar`. Because Explore uses `layout: false`, add the same composable-driven indicator or a compact overlay affordance inside `pages/explore.vue` or `ExploreHeaderOverlay`. Update route history UI so it can show session-level entries or link to a session-level picker without confusing variants. Use concise UI; avoid large nested cards.</action>
  <verify>node scripts/run-node-tests.mjs tests/server/ai-route-status.test.mjs tests/server/ai-route-client.test.mjs && rg -n "RouteGenerationIndicator|useRouteGenerationStatus|route-sessions|sessionId" components/app/route-generation-indicator.vue components/app/nav-bar.vue components/explore/route-history.vue pages/explore.vue</verify>
  <acceptance_criteria>
    - Active generation is visible outside the Explore route panel.
    - Completed/failed result has an obvious route back to Explore/session restore.
    - Explore layout=false page still exposes the indicator.
    - UI distinguishes "route sessions" from "route variants".
  </acceptance_criteria>
  <done>User can see and reopen background generation progress/results from the interface.</done>
</task>

</tasks>

<verification>
- [ ] node scripts/run-node-tests.mjs tests/server/ai-route-status.test.mjs
- [ ] node scripts/run-node-tests.mjs tests/server/ai-route-client.test.mjs tests/server/ai-route-restore.test.mjs
- [ ] pnpm test:server
- [ ] pnpm lint:source
- [ ] pnpm typecheck
</verification>

<success_criteria>
- GENLIFE-02 is satisfied by a global status composable and visible indicator.
- GENLIFE-03 is satisfied by session-level route history and explicit session restore.
- GENLIFE-06 is satisfied by authenticated, compact, user-scoped history/status APIs.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-route-generation-continuity-and-completion-notifications/05.1-02-SUMMARY.md`
</output>
