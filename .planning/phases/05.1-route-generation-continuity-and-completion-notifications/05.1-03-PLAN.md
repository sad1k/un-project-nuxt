---
phase: 05.1-route-generation-continuity-and-completion-notifications
plan: 03
type: execute
wave: 3
depends_on:
  - 05.1-01
  - 05.1-02
files_modified:
  - lib/notifications/route-generation-notifications.ts
  - lib/db/schema/route-notification.ts
  - lib/db/schema/index.ts
  - lib/db/queries/route-notification.ts
  - server/api/notifications/route-generation-subscription.post.ts
  - server/api/notifications/route-generation-subscription.delete.ts
  - composables/use-route-generation-notifications.ts
  - components/app/route-generation-indicator.vue
  - public/route-generation-sw.js
  - tests/server/route-generation-notifications.test.mjs
autonomous: false
requirements:
  - GENLIFE-04
  - GENLIFE-05
  - GENLIFE-06
user_setup:
  - "True browser push delivery requires VAPID configuration and a push sender implementation. Do not add a web-push dependency unless the user explicitly approves it during execution; in-app completion notification remains required either way."
must_haves:
  truths:
    - D-09 in-app completion notification is required when the app is open.
    - D-10 browser push is best-effort and must be gated by service worker support, permission, VAPID config, and saved subscription.
    - D-11 notification text must not expose private route details, raw diary context, prompts, or provider errors.
    - D-12 notification subscription data is user-owned and revocable.
  artifacts:
    - lib/notifications/route-generation-notifications.ts
    - composables/use-route-generation-notifications.ts
    - tests/server/route-generation-notifications.test.mjs
  key_links:
    - route-generation status transitions trigger in-app notification through the composable.
    - server notification helpers store delivery/fallback state without overclaiming browser push.
    - optional service worker/subscription path is separated from the required in-app fallback.
---

<objective>
Add completion notifications for route generation with a reliable in-app path and a gated browser-push path.

Purpose: Users should be told when a background route finishes, while the app stays honest about browser push prerequisites.
Output: Notification model/helpers, in-app completion notification, optional push subscription endpoints/service worker shell, and tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/05.1-route-generation-continuity-and-completion-notifications/05.1-CONTEXT.md
@.planning/phases/05.1-route-generation-continuity-and-completion-notifications/05.1-RESEARCH.md
@.planning/phases/05.1-route-generation-continuity-and-completion-notifications/05.1-01-SUMMARY.md
@.planning/phases/05.1-route-generation-continuity-and-completion-notifications/05.1-02-SUMMARY.md
@components/notifications.vue
@components/app/route-generation-indicator.vue
@composables/use-route-generation-status.ts
@lib/db/schema/auth.ts
@lib/db/schema/ai-route.ts
@lib/db/queries/ai-route.ts
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-051-08 | Notification leaks private city/diary/provider details on lock screen. | high | Use generic title/body with session link only; keep details inside authenticated app. |
| T-051-09 | App claims push support without permission/subscription/sender. | high | Model push capability explicitly and fall back to in-app status; tests assert gated behavior. |
| T-051-10 | Push subscription endpoint can be written for another user. | high | Subscription endpoints use `defineAuthenticatedHandler` and store `userId` from session only. |
| T-051-11 | Notification spam on repeated polling. | medium | Track per-session notified state and only notify once per terminal transition. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add notification behavior tests first</name>
  <files>tests/server/route-generation-notifications.test.mjs</files>
  <read_first>tests/server/ai-route-status.test.mjs, components/notifications.vue, .planning/phases/05.1-route-generation-continuity-and-completion-notifications/05.1-RESEARCH.md</read_first>
  <action>Create source-level tests for route generation notification behavior. Cover: in-app notification composable exists; completion/failure notifications are emitted once per terminal transition; notification text is generic and does not include raw request context/provider details; push subscription endpoints are authenticated/user-owned; browser push delivery is gated by support/permission/subscription/sender configuration and falls back when absent.</action>
  <verify>node scripts/run-node-tests.mjs tests/server/route-generation-notifications.test.mjs</verify>
  <acceptance_criteria>
    - Tests cover GENLIFE-04, GENLIFE-05, and GENLIFE-06.
    - Tests do not require real browser Notification API, service worker, or push network.
    - Tests make it impossible to silently claim push delivery without gating/fallback logic.
  </acceptance_criteria>
  <done>Notification requirements are locked before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Add notification state model and safe message helpers</name>
  <files>lib/notifications/route-generation-notifications.ts, lib/db/queries/ai-route.ts</files>
  <read_first>lib/db/queries/ai-route.ts, lib/db/schema/ai-route.ts, .planning/phases/05.1-route-generation-continuity-and-completion-notifications/05.1-CONTEXT.md</read_first>
  <action>Create pure notification helpers that derive safe in-app/browser notification payloads from compact route session summaries. Add query helper support to mark `notificationStatus` or equivalent lifecycle metadata as pending/sent/fallback/failed per session or variant. Notification text must be generic, e.g. "Your route is ready" or "Route generation failed", with a route-session link/id for authenticated app navigation; do not include raw diary text, full prompt, provider error body, or sensitive city details in push text.</action>
  <verify>node scripts/run-node-tests.mjs tests/server/route-generation-notifications.test.mjs</verify>
  <acceptance_criteria>
    - Notification payloads are generated by pure helpers.
    - Terminal transitions can be marked as notified once.
    - Notification state is stored in user-owned route lifecycle data.
    - Browser notification text is safe for lock-screen display.
  </acceptance_criteria>
  <done>Route completion has a safe notification state and message contract.</done>
</task>

<task type="auto">
  <name>Task 3: Add in-app completion notifications</name>
  <files>composables/use-route-generation-notifications.ts, components/app/route-generation-indicator.vue</files>
  <read_first>composables/use-route-generation-status.ts, components/notifications.vue, components/app/route-generation-indicator.vue</read_first>
  <action>Create `useRouteGenerationNotifications()` that watches the global route generation status composable and emits an in-app notification/toast when a session transitions to completed or failed. Integrate the composable with the global indicator or default app surface. Prevent repeated notifications for the same terminal session by tracking ids in reactive state and persisted notification status where available.</action>
  <verify>node scripts/run-node-tests.mjs tests/server/route-generation-notifications.test.mjs tests/server/ai-route-status.test.mjs</verify>
  <acceptance_criteria>
    - In-app completion/failure notification works while the app is open.
    - Notification links/actions open the relevant Explore session.
    - Re-polling does not duplicate the same terminal notification.
    - UI remains compact and does not add a broad notification center.
  </acceptance_criteria>
  <done>Users in the app are notified when a background generation finishes.</done>
</task>

<task type="checkpoint">
  <name>Task 4: Gate browser push delivery implementation</name>
  <files>public/route-generation-sw.js, server/api/notifications/route-generation-subscription.post.ts, server/api/notifications/route-generation-subscription.delete.ts, lib/db/schema/route-notification.ts, lib/db/queries/route-notification.ts</files>
  <read_first>.planning/phases/05.1-route-generation-continuity-and-completion-notifications/05.1-RESEARCH.md, nuxt.config.ts, package.json</read_first>
  <action>Implement the narrow browser push groundwork only to the extent allowed without unapproved dependencies: service worker registration shell, permission/subscription capability detection, authenticated subscription save/delete endpoints, and user-owned subscription schema/query helpers. If true server push delivery requires adding `web-push` or another push provider dependency, stop at a checkpoint and ask for explicit dependency approval. If approval is not granted, record GENLIFE-05 as subscription-ready but push-delivery blocked, with in-app notification fallback complete.</action>
  <verify>node scripts/run-node-tests.mjs tests/server/route-generation-notifications.test.mjs && rg -n "serviceWorker|PushManager|Notification|subscription|defineAuthenticatedHandler" public/route-generation-sw.js server/api/notifications lib/db/queries/route-notification.ts</verify>
  <acceptance_criteria>
    - Subscription save/delete endpoints are authenticated and user-owned.
    - Browser API access is feature-detected and secure-context aware.
    - No dependency is added without explicit approval.
    - If push delivery is not implemented, the summary names the blocker and the in-app fallback remains functional.
  </acceptance_criteria>
  <done>Browser push capability is honestly gated and subscription groundwork is in place where feasible.</done>
</task>

</tasks>

<verification>
- [ ] node scripts/run-node-tests.mjs tests/server/route-generation-notifications.test.mjs
- [ ] node scripts/run-node-tests.mjs tests/server/ai-route-status.test.mjs
- [ ] pnpm test:server
- [ ] pnpm lint:source
- [ ] pnpm typecheck
- [ ] rg -n "web-push|@webpush|push" package.json pnpm-lock.yaml confirms no unapproved dependency was added, or summary records explicit approval.
</verification>

<success_criteria>
- GENLIFE-04 is satisfied by in-app completion/failure notification.
- GENLIFE-05 is either satisfied by an approved push sender implementation or explicitly blocked with subscription-ready fallback and no false claim.
- GENLIFE-06 is satisfied by user-owned notification state/subscription handling and safe message text.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-route-generation-continuity-and-completion-notifications/05.1-03-SUMMARY.md`
</output>
