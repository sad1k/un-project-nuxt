---
phase: 05-place-intelligence-and-weather-tips
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/explore/place-intelligence.ts
  - lib/explore/place-intelligence-providers.ts
  - lib/db/queries/place-intelligence.ts
  - server/api/explore/place-intelligence.get.ts
  - lib/env.ts
  - tests/server/place-intelligence.test.mjs
autonomous: true
requirements:
  - PLACE-01
  - PLACE-02
  - PLACE-03
  - PLACE-04
  - PLACE-05
  - PLACE-06
user_setup:
  - "Google Places enrichment is optional. If GOOGLE_PLACES_API_KEY is absent, provider fields must degrade to explicit missing-data placeholders and app/route data."
must_haves:
  truths:
    - D-01/D-02/D-03 place intelligence includes a photo-first data model and explicit missing-data slots for popup sections.
    - D-04/D-06/D-07 external provider fields are optional, sourced, confidence-aware, and never treated as guaranteed.
    - D-05 AI-enriched summaries are grounded in available route/provider/app fields and must not fabricate facts.
    - D-08/D-09/D-10 community visit and likely/currently-there signals use aggregate app data only and expose uncertainty.
  artifacts:
    - lib/explore/place-intelligence.ts
    - lib/explore/place-intelligence-providers.ts
    - lib/db/queries/place-intelligence.ts
    - server/api/explore/place-intelligence.get.ts
    - tests/server/place-intelligence.test.mjs
  key_links:
    - server/api/explore/place-intelligence.get.ts authenticates the user and shapes route/provider/app data through lib/explore/place-intelligence.ts.
    - lib/db/queries/place-intelligence.ts aggregates location/locationLog data without exposing user identities.
    - lib/explore/place-intelligence-providers.ts keeps provider credentials server-side and uses explicit field selection.
---

<objective>
Create the server-side place intelligence contract and endpoint for rich generated-route popups.

Purpose: Photo-first popup UI needs a stable, sourced, missing-data-aware data shape before Mapbox rendering changes.
Output: Place intelligence types/helpers, optional provider adapter boundary, app-community aggregate query helper, authenticated endpoint, env schema update, and focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-place-intelligence-and-weather-tips/05-CONTEXT.md
@.planning/phases/05-place-intelligence-and-weather-tips/05-RESEARCH.md
@.planning/phases/03-ai-route-generation-and-streaming/03-HANDOFF.md
@.planning/phases/04-animated-map-route-experience/04-CONTEXT.md
@lib/ai/route-contract.ts
@lib/db/schema/ai-route.ts
@lib/db/schema/location.ts
@lib/db/schema/location-log.ts
@lib/db/schema/location-log-image.ts
@lib/db/queries/ai-route.ts
@lib/db/queries/explore-context.ts
@server/api/explore/candidate-places.get.ts
@lib/env.ts
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-05-01 | Provider or AI enrichment fabricates unsupported reviews, ratings, cost, or presence. | high | Model source/confidence per field, render missing-data slots, and test that weak/empty inputs do not produce claims. |
| T-05-02 | Community presence exposes user identities or raw diary history. | high | Aggregate counts/signals only; endpoint returns no user ids, names, diary text, or raw log rows. |
| T-05-03 | Provider key is exposed through public runtime config or browser requests. | high | Add optional server-only env key and keep Google Places fetches in Nitro/server libraries only. |
| T-05-04 | Provider field masks over-fetch expensive/sensitive data. | medium | Use explicit Place Details fields for display needs; do not use wildcard field masks in production code. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add place intelligence tests first</name>
  <files>tests/server/place-intelligence.test.mjs</files>
  <read_first>tests/server/explore-route-map.test.mjs, .planning/phases/05-place-intelligence-and-weather-tips/05-CONTEXT.md, .planning/phases/05-place-intelligence-and-weather-tips/05-RESEARCH.md</read_first>
  <action>Create focused `node:test` coverage for the new place intelligence model/helpers. Cover: photo-first fields exist; missing photo/review/rating/cost/community sections create explicit missing-data slots; provider rating/review/cost fields keep source/confidence metadata; AI summary is represented as enriched/sourced content, not a raw unsourced fact; community likely/currently-there signal is absent or low-confidence when aggregate app data is insufficient; returned community data contains no user identity fields.</action>
  <verify>node scripts/run-node-tests.mjs tests/server/place-intelligence.test.mjs fails before implementation and passes after Tasks 2-4.</verify>
  <acceptance_criteria>
    - Tests mention D-01 through D-10.
    - Tests do not call Google Places, OpenAI, Mapbox, Turso remote, or any network.
    - Tests assert missing-data placeholders/slots are part of the shaped response.
  </acceptance_criteria>
  <done>Place intelligence behavior is locked before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Define place intelligence model and shaping helpers</name>
  <files>lib/explore/place-intelligence.ts</files>
  <read_first>lib/ai/route-contract.ts, lib/explore/route-map.ts, tests/server/place-intelligence.test.mjs</read_first>
  <action>Create `lib/explore/place-intelligence.ts` with Zod schemas and exported TypeScript types for `PlaceIntelligence`, `PlacePhoto`, `PlaceReviewSnippet`, `PlaceRating`, `PlaceCostSignal`, `PlaceCommunitySignal`, `PlaceWeatherReference`, `PlaceMissingDataSlot`, and `PlaceDataSource`. Add pure helpers to merge route point data, provider data, app aggregate data, and optional AI summary into a display-ready object. Include explicit missing slots for unavailable photo, reviews, rating, cost, and community signal. Reuse `estimatedPriceLevel`, `priceConfidence`, and `priceSource` from the Phase 3 route point when provider cost is absent. Do not import Vue, Mapbox, database, env, or fetch.</action>
  <verify>node scripts/run-node-tests.mjs tests/server/place-intelligence.test.mjs</verify>
  <acceptance_criteria>
    - `lib/explore/place-intelligence.ts` exports schemas/types and pure shaping helpers.
    - Missing data is represented explicitly, not by silently omitted UI sections.
    - AI summary fields include source/confidence and cannot replace provider/app facts without source metadata.
    - The file contains no browser globals, database imports, provider fetches, env reads, or console logging.
  </acceptance_criteria>
  <done>Place intelligence has a pure, testable display model.</done>
</task>

<task type="auto">
  <name>Task 3: Add optional provider adapter boundary</name>
  <files>lib/explore/place-intelligence-providers.ts, lib/env.ts</files>
  <read_first>lib/env.ts, server/api/explore/candidate-places.get.ts, .planning/phases/05-place-intelligence-and-weather-tips/05-RESEARCH.md, lib/explore/place-intelligence.ts</read_first>
  <action>Add optional server-only env support for `GOOGLE_PLACES_API_KEY`. Create `lib/explore/place-intelligence-providers.ts` with native-fetch provider functions that can search/details a route point against Google Places when configured. Use explicit field masks for only popup-needed fields: display name/address where needed, photos, rating, reviews, price level, and generative summary if requested. Return normalized provider data matching `lib/explore/place-intelligence.ts`. If the key is absent or the provider fails, return an unavailable provider result without throwing user-visible secrets. Do not add a new runtime dependency.</action>
  <verify>rg -n "GOOGLE_PLACES_API_KEY|X-Goog-FieldMask|places.googleapis.com|fetch" lib/env.ts lib/explore/place-intelligence-providers.ts</verify>
  <acceptance_criteria>
    - `GOOGLE_PLACES_API_KEY` is optional and server-only.
    - Provider code lives outside public runtime config and client components.
    - Provider requests use explicit fields/field masks and native `fetch`.
    - Provider failures are normalized into unavailable/missing-data states.
  </acceptance_criteria>
  <done>External rich place data can be fetched server-side when configured and skipped safely when not.</done>
</task>

<task type="auto">
  <name>Task 4: Add app-community aggregate query helper</name>
  <files>lib/db/queries/place-intelligence.ts</files>
  <read_first>lib/db/queries/explore-context.ts, lib/db/schema/location.ts, lib/db/schema/location-log.ts, lib/db/schema/location-log-image.ts, lib/explore/place-intelligence.ts</read_first>
  <action>Create app-community aggregate helpers for Phase 5. Inputs should include route point name and coordinates plus thresholds/window options. Return aggregate visit count, unique visitor count or safe bucket if direct distinct-user count is impractical, recent visit count, likely/currently-there label, confidence, and source metadata. Do not return user identities, diary descriptions, raw visit rows, or image keys unless explicitly needed for app-owned popup photos. If the query cannot support a likely/currently-there signal, return missing/unsupported rather than a fabricated claim.</action>
  <verify>rg -n "locationLog|count|userId|likely|currently|description" lib/db/queries/place-intelligence.ts</verify>
  <acceptance_criteria>
    - Query helper returns aggregate community data only.
    - No exported function returns raw `userId`, user profile fields, diary text, or raw log rows.
    - Recent presence uses a documented time window and confidence label.
    - Weak or empty app data produces no likely/currently-there claim.
  </acceptance_criteria>
  <done>App-community visit and presence signals can be computed without exposing personal records.</done>
</task>

<task type="auto">
  <name>Task 5: Add authenticated place intelligence endpoint</name>
  <files>server/api/explore/place-intelligence.get.ts, lib/db/queries/ai-route.ts</files>
  <read_first>utils/define-authenticated-handler.ts, server/api/explore/context.get.ts, lib/db/queries/ai-route.ts, lib/explore/place-intelligence.ts, lib/explore/place-intelligence-providers.ts, lib/db/queries/place-intelligence.ts</read_first>
  <action>Create `server/api/explore/place-intelligence.get.ts`. Authenticate with `defineAuthenticatedHandler`. Validate query params for `variantId`, `routePointId`, `name`, `lat`, and `long` or use the safest existing route-point lookup shape. If route point identifiers are supplied, verify the route point belongs to the authenticated user before enrichment; add a small query helper to `lib/db/queries/ai-route.ts` if needed. Fetch provider data when configured, aggregate app-community data, merge route/provider/app/AI-summary fields through `lib/explore/place-intelligence.ts`, and return the display-ready object. Never return provider raw payloads, secrets, headers, prompts, or raw personal context.</action>
  <verify>node scripts/run-node-tests.mjs tests/server/place-intelligence.test.mjs && rg -n "defineAuthenticatedHandler|PlaceIntelligence|GOOGLE_PLACES_API_KEY|raw|headers" server/api/explore/place-intelligence.get.ts lib/db/queries/ai-route.ts</verify>
  <acceptance_criteria>
    - Endpoint validates inputs before provider/query work.
    - Endpoint verifies authenticated ownership for route point identifiers.
    - Endpoint returns a shaped place intelligence object with missing-data slots.
    - Endpoint does not expose raw provider payloads or raw app-community records.
  </acceptance_criteria>
  <done>Explore can request rich place intelligence for a generated route point.</done>
</task>

</tasks>

<verification>
- [ ] node scripts/run-node-tests.mjs tests/server/place-intelligence.test.mjs
- [ ] pnpm test:server
- [ ] pnpm lint:source
- [ ] pnpm typecheck
- [ ] rg -n "GOOGLE_PLACES_API_KEY" nuxt.config.ts components pages composables returns no matches
- [ ] rg -n "console\\.|process\\.env|rawProvider|headers" lib/explore/place-intelligence.ts lib/explore/place-intelligence-providers.ts server/api/explore/place-intelligence.get.ts reports no unsafe runtime logging/exposure
</verification>

<success_criteria>
- PLACE-01 has a server response shape for rich popup place information.
- PLACE-02 through PLACE-04 have sourced/missing-aware fields for photos, reviews/ratings, and cost.
- PLACE-05 and PLACE-06 have aggregate app-community signal support without identity exposure or fabricated presence.
- CONTEXT decisions D-01 through D-10 are represented in executable tasks.
</success_criteria>

<output>
After completion, create `.planning/phases/05-place-intelligence-and-weather-tips/05-01-SUMMARY.md`
</output>
