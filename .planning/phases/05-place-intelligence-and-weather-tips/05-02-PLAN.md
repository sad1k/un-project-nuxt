---
phase: 05-place-intelligence-and-weather-tips
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - composables/use-place-intelligence.ts
  - components/explore/place-popup.ts
  - components/explore/route-marker.ts
  - composables/use-mapbox.ts
  - pages/explore.vue
  - tests/server/place-popup-renderer.test.mjs
autonomous: true
requirements:
  - PLACE-01
  - PLACE-02
  - PLACE-03
  - PLACE-04
  - PLACE-05
  - PLACE-06
user_setup: []
must_haves:
  truths:
    - D-01 rich place popups are photo-first when a real photo is available.
    - D-02 missing photos, reviews, ratings, cost, and community signals render explicit missing-data UI.
    - D-03 place name, route rationale, actions, and practical details support the photo-first hierarchy.
    - D-04/D-05/D-06/D-07 sourced provider/app/AI data is labeled or confidence-aware and never shown as raw JSON.
    - D-08/D-09/D-10 likely/currently-there and visit-count UI communicate app-data uncertainty and never claim guaranteed live occupancy.
  artifacts:
    - composables/use-place-intelligence.ts
    - components/explore/place-popup.ts
    - components/explore/route-marker.ts
    - composables/use-mapbox.ts
    - pages/explore.vue
    - tests/server/place-popup-renderer.test.mjs
  key_links:
    - components/explore/place-popup.ts renders the PlaceIntelligence model from lib/explore/place-intelligence.ts.
    - use-mapbox.ts requests or receives popup HTML for route markers without embedding provider logic.
    - pages/explore.vue wires generated route points to place intelligence fetching.
---

<objective>
Render photo-first rich place popups for generated route markers.

Purpose: Users should be able to click a generated route place and inspect visual, practical, sourced, and community-aware details without raw provider data or silent gaps.
Output: Client place-intelligence composable, safe popup renderer, marker/map/page integration, and renderer tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-place-intelligence-and-weather-tips/05-CONTEXT.md
@.planning/phases/05-place-intelligence-and-weather-tips/05-RESEARCH.md
@.planning/phases/05-place-intelligence-and-weather-tips/05-01-SUMMARY.md
@lib/explore/place-intelligence.ts
@server/api/explore/place-intelligence.get.ts
@components/explore/route-marker.ts
@composables/use-mapbox.ts
@pages/explore.vue
@lib/explore/route-map.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add popup renderer tests first</name>
  <files>tests/server/place-popup-renderer.test.mjs</files>
  <read_first>tests/server/place-intelligence.test.mjs, lib/explore/place-intelligence.ts, components/explore/route-marker.ts</read_first>
  <action>Create focused `node:test` source/pure-renderer coverage for a new popup renderer. Cover photo-first ordering; text escaping for provider/user/AI strings; missing-data placeholders for each missing slot; display of rating/reviews/cost/source labels when present; display of app-community visit and likely/currently-there uncertainty; no raw JSON payload rendering.</action>
  <verify>node scripts/run-node-tests.mjs tests/server/place-popup-renderer.test.mjs fails before implementation and passes after Task 2.</verify>
  <acceptance_criteria>
    - Tests inspect or import a popup renderer without requiring Mapbox/browser globals.
    - Tests include an unsafe string fixture and assert it is escaped.
    - Tests mention photo-first and missing-data placeholders.
  </acceptance_criteria>
  <done>Popup rendering requirements are protected before Mapbox integration.</done>
</task>

<task type="auto">
  <name>Task 2: Implement safe photo-first popup renderer</name>
  <files>components/explore/place-popup.ts</files>
  <read_first>lib/explore/place-intelligence.ts, tests/server/place-popup-renderer.test.mjs, components/explore/route-marker.ts</read_first>
  <action>Create `components/explore/place-popup.ts` as a pure renderer for `PlaceIntelligence`. Export an HTML-rendering helper and a text escaping helper. Render a photo-first section when a photo URL exists; otherwise render the missing photo slot. Render name, route rationale/AI summary, rating, review snippets, cost, community visit count, likely/currently-there signal, source/confidence labels, and compact actions. Render missing-data slots visibly for unavailable sections. Keep markup compact enough for a Mapbox popup and avoid Vue-only APIs so the renderer can be tested with Node.</action>
  <verify>node scripts/run-node-tests.mjs tests/server/place-popup-renderer.test.mjs</verify>
  <acceptance_criteria>
    - Renderer escapes all dynamic strings before HTML insertion.
    - Photo section is first in the rendered markup.
    - Missing data slots render for absent photo/reviews/rating/cost/community data.
    - Renderer does not expose raw provider payloads, raw JSON, or secrets.
  </acceptance_criteria>
  <done>Photo-first popup HTML can be rendered safely from the place intelligence model.</done>
</task>

<task type="auto">
  <name>Task 3: Add client place intelligence state</name>
  <files>composables/use-place-intelligence.ts</files>
  <read_first>composables/use-ai-route-session.ts, lib/explore/route-map.ts, lib/explore/place-intelligence.ts, server/api/explore/place-intelligence.get.ts</read_first>
  <action>Create a composable that fetches `/api/explore/place-intelligence` for generated route points on demand. Cache by active variant id plus route point id/source id so hovering/clicking a marker does not repeatedly fetch the same data. Track loading, loaded, error, and unavailable states. Do not fetch for `current-location` or `user-place` markers unless explicitly supported by the endpoint. Use `$fetch`/Nuxt patterns already present in Explore composables.</action>
  <verify>rg -n "usePlaceIntelligence|place-intelligence|variantId|routePointId|cache" composables/use-place-intelligence.ts</verify>
  <acceptance_criteria>
    - Composable exposes a way to request/get intelligence for a generated route point.
    - Cache key includes route variant/session context so route variants do not cross-contaminate.
    - Provider/network errors degrade to unavailable popup data rather than breaking marker rendering.
    - No secrets or provider URLs are referenced in client code.
  </acceptance_criteria>
  <done>Client can lazily load popup data for route points.</done>
</task>

<task type="auto">
  <name>Task 4: Wire rich popups into Mapbox route markers</name>
  <files>components/explore/route-marker.ts, composables/use-mapbox.ts, pages/explore.vue</files>
  <read_first>components/explore/place-popup.ts, composables/use-place-intelligence.ts, components/explore/route-marker.ts, composables/use-mapbox.ts, pages/explore.vue</read_first>
  <action>Replace the minimal popup HTML path for generated route markers with rich place popup rendering. Keep marker creation compact, but allow `use-mapbox.ts` or `pages/explore.vue` to provide an async/intelligence-backed popup content resolver for generated route points. Show an immediate loading or missing-data popup state while enrichment loads. Preserve existing distinct marker styling and cleanup behavior. Current-location and user-place markers can keep simple labels unless place intelligence support is explicitly available.</action>
  <verify>rg -n "createPlacePopup|usePlaceIntelligence|setHTML|mouseenter|click|popup" components/explore/route-marker.ts composables/use-mapbox.ts pages/explore.vue</verify>
  <acceptance_criteria>
    - Clicking or hovering a generated place can show rich place intelligence.
    - Initial/loading/unavailable popup states are visible and do not throw.
    - Popup content never shows raw provider JSON or route event JSON.
    - Existing marker cleanup still removes popups/markers on route redraw and destroy.
  </acceptance_criteria>
  <done>Generated route markers open rich photo-first place popups.</done>
</task>

<task type="auto">
  <name>Task 5: Run popup-focused verification</name>
  <files>tests/server/place-popup-renderer.test.mjs, components/explore/place-popup.ts, components/explore/route-marker.ts, composables/use-mapbox.ts, pages/explore.vue</files>
  <action>Run focused tests and source verification for popup behavior. Then run the broader server test suite, lint, and typecheck. If full lint/typecheck fail due to unrelated existing source issues, record the exact blockers in `05-02-SUMMARY.md` and still report focused test results.</action>
  <verify>node scripts/run-node-tests.mjs tests/server/place-popup-renderer.test.mjs; pnpm test:server; pnpm lint:source; pnpm typecheck</verify>
  <acceptance_criteria>
    - Popup renderer tests pass.
    - No client source references server-only provider keys.
    - Any broader verification failures are recorded with exact commands and blockers.
  </acceptance_criteria>
  <done>Popup implementation has focused verification evidence.</done>
</task>

</tasks>

<verification>
- [ ] node scripts/run-node-tests.mjs tests/server/place-popup-renderer.test.mjs
- [ ] pnpm test:server
- [ ] pnpm lint:source
- [ ] pnpm typecheck
- [ ] rg -n "GOOGLE_PLACES_API_KEY|places.googleapis.com|OPENAI_API_KEY" components pages composables returns no matches
</verification>

<success_criteria>
- PLACE-01 is visible through clickable/hoverable generated-place popups.
- PLACE-02 through PLACE-04 appear photo-first with photos/reviews/ratings/cost when available and missing-data placeholders otherwise.
- PLACE-05 and PLACE-06 appear as app-community signals with uncertainty and no fabricated live occupancy.
- CONTEXT decisions D-01 through D-10 are visible in UI behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/05-place-intelligence-and-weather-tips/05-02-SUMMARY.md`
</output>
